#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	УзелОбмена = Параметры.УзелОбмена;
	
	НомераСообщений = ИТК_РегистрацияИзмененийНаУзлах.НомераСообщенийУзлаОбмена(УзелОбмена);
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, НомераСообщений);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаписатьИзменения(Команда)
	
	ЗаписатьНомераСообщений();
	Закрыть(Истина);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗаписатьНомераСообщений()
	
	ПараметрыУзла = Новый Структура;
	ПараметрыУзла.Вставить("НомерПринятого", НомерПринятого);
	ПараметрыУзла.Вставить("НомерОтправленного", НомерОтправленного);
	
	ИТК_РегистрацияИзмененийНаУзлах.УстановитьПараметрыУзлаОбмена(УзелОбмена, ПараметрыУзла);
	
КонецПроцедуры

#КонецОбласти
