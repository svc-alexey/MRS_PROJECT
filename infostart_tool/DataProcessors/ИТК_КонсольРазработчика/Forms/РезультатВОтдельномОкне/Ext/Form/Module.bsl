#Область ОписаниеПеременных

&НаКлиенте
Перем ПоследняяАктивнаяОбласть; // Для функции выделение активной строки

#КонецОбласти

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ИТК_ТабличныйДокумент.ИнициализироватьФорму(ЭтотОбъект, Истина);
	
	Заголовок = СтрШаблон(НСтр("ru = 'Результат (%1)';
								|en = 'Result (%1)'"), Параметры.Имя);
	
	ТекущийРезультат = Новый Структура("Адрес, АдресДанныхРасшифровки, АдресСхемыКомпоновкиДанных");
	
	ТабличныйДокументРезультат.Вывести(ПолучитьИзВременногоХранилища(Параметры.АдресРезультата));
	ТекущийРезультат.Адрес = ПоместитьВоВременноеХранилище(ТабличныйДокументРезультат, УникальныйИдентификатор);
	
	ДанныеРасшифровки = ПолучитьИзВременногоХранилища(Параметры.АдресДанныхРасшифровки);
	ТекущийРезультат.АдресДанныхРасшифровки = ПоместитьВоВременноеХранилище(ДанныеРасшифровки, УникальныйИдентификатор);
	
	Если Параметры.Свойство("АдресСхемыКомпоновкиДанных") Тогда
		
		СхемаКомпоновкиДанных = ПолучитьИзВременногоХранилища(Параметры.АдресСхемыКомпоновкиДанных);
		ТекущийРезультат.АдресСхемыКомпоновкиДанных = ПоместитьВоВременноеХранилище(СхемаКомпоновкиДанных, УникальныйИдентификатор);
		
	КонецЕсли;
	
	ИТК_Настройки.ИнициализироватьФорму(ЭтотОбъект, ИТК_Перечисления.ИнструментКонсольРазработчика());
	Элементы.ГруппаНеВсеСтрокиВыведены.Видимость = (Параметры.ВсеСтрокиВыведены = Ложь);
	
	// Фиксация шапки
	Элементы.КнопкаФиксацияШапки.Доступность = Параметры.ФиксацияШапкиДоступность;
	Элементы.КнопкаФиксацияШапки.Пометка = Параметры.ФиксацияШапки;
	ИТК_ТабличныйДокументКлиентСервер.ЗафиксироватьШапку(ТабличныйДокументРезультат, Параметры.ФиксацияШапки);
	
	ИТК_ТабличныйДокументКлиентСервер.ОбновитьЭлементыПодменюУровеньГруппировок(
		Элементы.ТабличныйДокументРезультатКонтекстноеМенюУровеньГруппировки, ТабличныйДокументРезультат);
	
	ОбновитьЭлементыФормы(ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	ИзмененыНастройки = ИТК_НастройкиКлиент.ОбработкаОповещения(ЭтотОбъект, ИмяСобытия, ИТК_Перечисления.ИнструментКонсольРазработчика());
	Если ИзмененыНастройки Тогда 
		
		ОбновитьЭлементыФормы(ЭтотОбъект);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура РезультатПриАктивизацииОбласти(Элемент)
	
	ИТК_ТабличныйДокументКлиент.ОбработчикПриАктивизацииОбласти(ЭтотОбъект, Элемент, ТабличныйДокументРезультат, ПоследняяАктивнаяОбласть, ТекущийРезультат.АдресДанныхРасшифровки);
	
КонецПроцедуры

&НаКлиенте
Процедура РезультатВыбор(Элемент, Область, СтандартнаяОбработка)
	
	ИТК_ТабличныйДокументКлиент.ОбработчикВыбор(ЭтотОбъект, ТекущийРезультат.АдресДанныхРасшифровки, Область, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПодключаемаяКомандаЭкспортВ(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЭкспортФайлаЗакончен", ЭтотОбъект);
	ИТК_ФормаКлиент.ОткрытьФормуЭкспортВ(ЭтотОбъект, ОписаниеОповещения);
	
	
КонецПроцедуры

&НаКлиенте
Процедура ПодключаемаяКомандаРезультатВТабличномДокументе(Команда)

	ИТК_ТабличныйДокументКлиент.ОбработкаКомандыРезультатВТабличномДокументе(ЭтотОбъект, Команда,
			ТабличныйДокументРезультат, ТекущийРезультат.Адрес, ТекущийРезультат.АдресДанныхРасшифровки);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура РезультатПриАктивизацииОбластиПодключаемый() Экспорт
	
	ИТК_ТабличныйДокументКлиент.ПриАктивизацииОбластиОбработка(ЭтотОбъект, ТабличныйДокументРезультат);
	
КонецПроцедуры

&НаКлиенте
Процедура ЭкспортФайлаЗакончен(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ВыводРезультатаВТабличныйДокументНаСервер(ТабличныйДокумент, Результат.УдалитьЗаголовок);
	
	ИТК_ТабличныйДокументКлиент.ЭкспортФайлаЗакончен(ТабличныйДокумент, Результат, ПоследняяАктивнаяОбласть);
	
КонецПроцедуры

&НаСервере
Процедура ВыводРезультатаВТабличныйДокументНаСервер(ТабличныйДокумент, УдалитьЗаголовок)
	
	ТабличныйДокумент.Вывести(ТабличныйДокументРезультат);
	
	Если УдалитьЗаголовок Тогда
		Область = ТабличныйДокумент.Область("R1:R2");
		ТабличныйДокумент.УдалитьОбласть(Область, ТипСмещенияТабличногоДокумента.ПоВертикали);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура ОбновитьЭлементыФормы(Форма)
	
	Элементы = Форма.Элементы;
	
	РасчетСуммыВыделенныхЯчеек = ИТК_НастройкиКлиентСервер.ЗначениеНастройки(Форма, "РасчетСуммыВыделенныхЯчеек");
	Элементы.ГруппаСумма.Видимость = РасчетСуммыВыделенныхЯчеек;
	
КонецПроцедуры

#КонецОбласти
