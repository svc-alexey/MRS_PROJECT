#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Шаг = 1;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбновитьЭлементыУправления();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ПолноеИмяФайлаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	ДиалогВыбораФайла = Новый ДиалогВыбораФайла (РежимДиалогаВыбораФайла.Открытие);
	ДиалогВыбораФайла.Каталог = ПолноеИмяФайла;
	ДиалогВыбораФайла.ПолноеИмяФайла = ПолноеИмяФайла;
	ДиалогВыбораФайла.Фильтр = ФильтрЗагрузкиДанныхИзТабличногоДокумента();
	ДиалогВыбораФайла.Заголовок = НСтр("ru = 'Выбор файла';
										|en = 'Select file'");
	ДиалогВыбораФайла.МножественныйВыбор = Ложь;
	
	ДиалогВыбораФайла.Показать(Новый ОписаниеОповещения("ВыборФайлаЗакончен", ЭтотОбъект));
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПримерMXL(Команда)
	
	ПолучитьФайл(ПримерФайлаMXL(), НСтр("ru = 'Пример.mxl';
										|en = 'Sample.mxl'"));
	
КонецПроцедуры

&НаКлиенте
Процедура ПримерExcel(Команда)
	
	ПолучитьФайл(ПримерФайлаExcel(), НСтр("ru = 'Пример.xlsx';
											|en = 'Sample.xlsx'"));
	
КонецПроцедуры

&НаКлиенте
Процедура Назад(Команда)
	
	Шаг = Шаг - 1;
	ОбновитьЭлементыУправления();
	
КонецПроцедуры

&НаКлиенте
Процедура Далее(Команда)
	
	Если Шаг = 1 Тогда
		
		Если ЗначениеЗаполнено(ПолноеИмяФайла) Тогда 
			ЗагрузкаДанныхФайла();
		Иначе
			ТекстОшибки = НСтр("ru = 'Не выбран файл';
								|en = 'No file selected'");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ТекстОшибки) Тогда
			ПоказатьПредупреждение( , ТекстОшибки, , ИТК_КонсольРазработчикаКлиентСервер.Представление());
			ТекущийЭлемент = Элементы.ПолноеИмяФайла;
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	Шаг = Шаг + 1;
	ОбновитьЭлементыУправления();
	
КонецПроцедуры

&НаКлиенте
Процедура Загрузить(Команда)
	
	Результат = ЗагрузкаДанныхСервер();
	Закрыть(Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьВсе(Команда)
	
	ИТК_ФормаКлиент.УстановитьПометки(КолонкиФайла);
	
КонецПроцедуры

&НаКлиенте
Процедура СнятьВсе(Команда)
	
	ИТК_ФормаКлиент.УстановитьПометки(КолонкиФайла, Ложь);

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ПримерФайлаMXL()
	
	ПримерФайлаДвоичныеДанные = ПолучитьОбщийМакет("ИТК_ПримерФайлаMXLДляЗагрузкиВТаблицуЗначений_ru");
	Возврат ПоместитьВоВременноеХранилище(ПримерФайлаДвоичныеДанные);
	
КонецФункции

&НаСервере
Функция ПримерФайлаExcel()
	
	ПримерФайлаДвоичныеДанные = ПолучитьОбщийМакет("ИТК_ПримерФайлаExcelДляЗагрузкиВТаблицуЗначений_ru");
	Возврат ПоместитьВоВременноеХранилище(ПримерФайлаДвоичныеДанные);
	
КонецФункции

&НаКлиенте
Функция ФильтрЗагрузкиДанныхИзТабличногоДокумента()
	
	ПоддерживаемыеФорматы = Новый СписокЗначений;
	ПоддерживаемыеФорматы.Добавить("*.mxl", НСтр("ru = 'Файлы табличных документов от 1С';
												|en = 'Files of tabular documents from 1C'"));
	ПоддерживаемыеФорматы.Добавить("*.xls; *.xlsx", НСтр("ru = 'Файлы Excel';
														|en = 'Files Excel'"));
	
	Возврат ИТК_ОбщийКлиентСервер.ФильтрФайлов(ПоддерживаемыеФорматы);
	
КонецФункции

&НаКлиенте
Процедура ВыборФайлаЗакончен(ВыбранныеФайлы, ДополнительныеПараметры) Экспорт
	
	Если ВыбранныеФайлы = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ПолноеИмяФайла = ВыбранныеФайлы[0];
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьЭлементыУправления()
	
	ШагВыборФайла = 1;
	ШагЗагрузкаДанных = 2;
	
	ПервыйШаг = 1;
	ПоследнийШаг = ШагЗагрузкаДанных;
	
	
	Если Шаг = 1 Тогда
		
		ЗаголовокШага = НСтр("ru = 'Выбор файла';
							|en = 'Select file'");
		
	ИначеЕсли Шаг = ШагЗагрузкаДанных Тогда
		
		ЗаголовокШага = НСтр("ru = 'Загрузка данных';
							|en = 'Data loading'");
		
	Иначе
		
		ВызватьИсключение НСтр("ru = 'Непредсказуемое значение шага';
								|en = 'Unpredictable step value'");
		
	КонецЕсли;
	
	ЗаголовокФормы = НСтр("ru = 'Загрузка в таблицу значений';
							|en = 'Loading in the table of values'");
	Заголовок = СтрШаблон("Шаг %1/%2. %3 : %4", Шаг, ПоследнийШаг, ЗаголовокШага, ЗаголовокФормы);
	
	Элементы.ФормаНазад.Видимость = (Шаг > ПервыйШаг);
	Элементы.ФормаДалее.Видимость = (Шаг < ПоследнийШаг);
	Элементы.ФормаЗагрузить.Видимость = (Шаг = ПоследнийШаг);
	
	Элементы.ФормаДалее.КнопкаПоУмолчанию = (Шаг = ПервыйШаг);
	Элементы.ФормаЗагрузить.КнопкаПоУмолчанию = (Шаг = ПоследнийШаг);
	
	Элементы.СтраницаЗагрузкаФайла.Видимость = (Шаг = ШагВыборФайла);
	Элементы.СтраницаЗагрузкаДанных.Видимость = (Шаг = ШагЗагрузкаДанных);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузкаДанныхФайла()
	
	ДвоичныеДанныеФайла = Новый ДвоичныеДанные(ПолноеИмяФайла);
	АдресФайла = ИТК_ВременноеХранилище.Поместить(ДвоичныеДанныеФайла, АдресФайла, УникальныйИдентификатор);
	
	ЗагрузкаДанныхКолонокСервер();

КонецПроцедуры

&НаСервере
Процедура ЗагрузкаДанныхКолонокСервер()
	
	ТабличныйДокумент = ТабличныйДокументФайла();
	
	КолонкиФайла.Очистить();
	
	// Считываем колонки
	НомерНекорректногоИдентификатора = 1;
	Для НомерКолонки = 1 По ТабличныйДокумент.ШиринаТаблицы Цикл 
		
		Область = ТабличныйДокумент.Область(1, НомерКолонки);
		ИмяКолонки = Область.Текст;
		ИдентификаторКолонки = ИТК_Строки.КорректныйИдентификатор(ИмяКолонки);
		Если ИдентификаторКолонки = Неопределено Тогда
			ИдентификаторКолонки = СтрШаблон(НСтр("ru = 'НекорректныйИдентификатор%1';
													|en = 'InvalidID%1'"), НомерНекорректногоИдентификатора);
			НомерНекорректногоИдентификатора = НомерНекорректногоИдентификатора + 1;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ИмяКолонки) Тогда
			НоваяКолонка = КолонкиФайла.Добавить();
			НоваяКолонка.Идентификатор = ИдентификаторКолонки;
			НоваяКолонка.Пометка = Истина;
			НоваяКолонка.НомерКолонки = НомерКолонки;
		КонецЕсли;
		
	КонецЦикла;

КонецПроцедуры

&НаСервере
Функция ТабличныйДокументФайла()
	
	Файл = Новый Файл(ПолноеИмяФайла);
	Расширение = ВРег(Файл.Расширение);
	
	ДвоичныеДанныеФайла = ПолучитьИзВременногоХранилища(АдресФайла);
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТипФайлаТаблицы = Неопределено;
	
	ЧтениеПотоком = Ложь;
	Если Расширение = ".MXL" Тогда
		
		ЧтениеПотоком = Истина;
		ТипФайлаТаблицы = ТипФайлаТабличногоДокумента.MXL;
		
	ИначеЕсли Расширение = ".XLS" Тогда
		
		ТипФайлаТаблицы = ТипФайлаТабличногоДокумента.XLS;
		
	ИначеЕсли Расширение = ".XLSX" Тогда
		
		ТипФайлаТаблицы = ТипФайлаТабличногоДокумента.XLSX;
		
	Иначе
		
		ВызватьИсключение "Обработка этого формата файла не предусмотрена";
		
	КонецЕсли;
	
	Если ЧтениеПотоком Тогда
		
		ОбъектЧтения = ДвоичныеДанныеФайла.ОткрытьПотокДляЧтения();
		
	Иначе 
		
		ПолноеИмяФайлаВременногоФайла = ПолучитьИмяВременногоФайла(Расширение);
		ДвоичныеДанныеФайла.Записать(ПолноеИмяФайлаВременногоФайла);
		
		ОбъектЧтения = ПолноеИмяФайлаВременногоФайла;
		
	КонецЕсли;
	
	ТабличныйДокумент.Прочитать(ОбъектЧтения, СпособЧтенияЗначенийТабличногоДокумента.Значение, ТипФайлаТаблицы);
	
	Если Не ЧтениеПотоком Тогда
		УдалитьФайлы(ПолноеИмяФайлаВременногоФайла);
	КонецЕсли;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

&НаСервере
Функция ЗагрузкаДанныхСервер()
	
	Результат = Новый ТаблицаЗначений;
	ТабличныйДокумент = ТабличныйДокументФайла();
	
	// Загрузка колонок
	ЗагружаемыеКолонки = Новый ТаблицаЗначений;
	ЗагружаемыеКолонки.Колонки.Добавить("Идентификатор", ИТК_ТипыКлиентСервер.ОписаниеСтрока());
	ЗагружаемыеКолонки.Колонки.Добавить("Номер", ИТК_ТипыКлиентСервер.ОписаниеЧисло(10));
	ЗагружаемыеКолонки.Колонки.Добавить("Типы");
	ЗагружаемыеКолонки.Колонки.Добавить("МаксимальнаяДлинаСтроки", ИТК_ТипыКлиентСервер.ОписаниеЧисло(10));
	
	Для Каждого Колонка Из КолонкиФайла Цикл 
		
		Если Не Колонка.Пометка Тогда
			Продолжить;
		КонецЕсли;
		
		ЗагружаемаяКолонка = ЗагружаемыеКолонки.Добавить();
		ЗагружаемаяКолонка.Идентификатор = Колонка.Идентификатор;
		ЗагружаемаяКолонка.Номер = Колонка.НомерКолонки;
		ЗагружаемаяКолонка.Типы = Новый Соответствие;
		
		Результат.Колонки.Добавить(Колонка.Идентификатор, Новый ОписаниеТипов);
		
	КонецЦикла;
	
	// Загрузка строк
	НачальнаяСтрока = 2;
	Для НомерСтроки = НачальнаяСтрока По ТабличныйДокумент.ВысотаТаблицы Цикл 
			
		НоваяСтрока = Результат.Добавить();
		
		Для Каждого ЗагружаемаяКолонка Из ЗагружаемыеКолонки Цикл 
			
			Область = ТабличныйДокумент.Область(НомерСтроки, ЗагружаемаяКолонка.Номер);
			
			ЗначениеЯчейки = ЗначениеЯчейкиОбласти(Область);
			ТипЗначенияЯчейки = ТипЗнч(ЗначениеЯчейки);
			
			НоваяСтрока[ЗагружаемаяКолонка.Идентификатор] = ЗначениеЯчейки;
			ЗагружаемаяКолонка.Типы.Вставить(ТипЗначенияЯчейки, Истина);
			
			Если ТипЗначенияЯчейки = Тип("Строка") Тогда
				ЗагружаемаяКолонка.МаксимальнаяДлинаСтроки = Макс(ЗагружаемаяКолонка.МаксимальнаяДлинаСтроки, СтрДлина(ЗначениеЯчейки));
			КонецЕсли;
			
		КонецЦикла;
				
	КонецЦикла;
	
	// Получение результата
	Для Каждого ЗагружаемаяКолонка Из ЗагружаемыеКолонки Цикл 
		
		ИмяКолонки = ЗагружаемаяКолонка.Идентификатор;
		
		ТипыКолонки = Новый Массив;
		Для Каждого ЭлементТип Из ЗагружаемаяКолонка.Типы Цикл 
			
			ТипыКолонки.Добавить(ЭлементТип.Ключ);
			
		КонецЦикла;
		
		Если ЗагружаемаяКолонка.МаксимальнаяДлинаСтроки < 1000 Тогда
			ДопустимаяДлинаСтроки = ДопустимаяДлина.Фиксированная;
		Иначе
			ДопустимаяДлинаСтроки = ДопустимаяДлина.Переменная;
		КонецЕсли;
		КвалификаторыСтроки = Новый КвалификаторыСтроки(ЗагружаемаяКолонка.МаксимальнаяДлинаСтроки, ДопустимаяДлинаСтроки);
		
		ОписаниеТипов = Новый ОписаниеТипов(ТипыКолонки, , КвалификаторыСтроки);
		
		ИТК_ТаблицаЗначений.ИзменитьТипКолонки(Результат, ИмяКолонки, ОписаниеТипов); 
		
	КонецЦикла;
	
	Возврат ПоместитьВоВременноеХранилище(Результат);
	
КонецФункции

&НаСервере
Функция ЗначениеЯчейкиОбласти(Область)
	
	Если Область.Расшифровка = Неопределено Тогда
		Результат = Область.Текст;
	Иначе
		Результат = Область.Расшифровка;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти
