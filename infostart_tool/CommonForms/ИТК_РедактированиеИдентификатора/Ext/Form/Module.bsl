#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ИТК_ФормаКлиентСервер.Заголовок(ЭтотОбъект, Параметры.Заголовок);
	ИТК_ПроверкаОшибокВФорме.Инициализировать(ЭтотОбъект);
	
	Идентификатор = Параметры.НачальноеЗначение;
	Режим = Параметры.Режим;
	
	Если ЗначениеЗаполнено(Параметры.ЗанятыеИдентификаторы) Тогда
		ЗанятыеИдентификаторы.ЗагрузитьЗначения(Параметры.ЗанятыеИдентификаторы);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбновитьЭлементыФормы();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИдентификаторПриИзменении(Элемент)
	
	ПодключитьОбработчикОжидания("ПриИзмененииИдентификатораПослеОжидания", 0.1, Истина);	
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОшибкиОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	
	ИТК_ПроверкаОшибокВФормеКлиент.ОбработкаНавигационнойСсылки(ЭтотОбъект, Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	ОшибкиЗаполнения = ПроверкаЗаполнения();
	Если ЗначениеЗаполнено(ОшибкиЗаполнения) Тогда
		
		ИТК_ПроверкаОшибокВФормеКлиент.ТекущийЭлементПоПервой(ЭтотОбъект, ОшибкиЗаполнения);
		
	Иначе
		
		Закрыть(Идентификатор);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ОбновитьЭлементыФормы()
	
	ИТК_ПроверкаОшибокВФормеКлиент.Обновить(ЭтотОбъект, ПроверкаЗаполнения());
	
КонецПроцедуры
	
&НаКлиенте
Процедура ПриИзмененииИдентификатораПослеОжидания()

	ОбновитьЭлементыФормы();

КонецПроцедуры

&НаКлиенте
Функция ПроверкаЗаполнения()
	
	Результат = Новый СписокЗначений;
	ЗанятыеИдентификаторыМассив = ЗанятыеИдентификаторы.ВыгрузитьЗначения();
	
	Если Режим = ИТК_Перечисления.РежимРедактированияИдентификатораИдентификатор() Тогда
		
		ОшибкиИдентификатора = ИТК_Строки.ПроверкаИдентификатора(Идентификатор, ЗанятыеИдентификаторыМассив, "_");
		
	ИначеЕсли Режим = ИТК_Перечисления.РежимРедактированияИдентификатораИдентификаторВременнойТаблицы() Тогда
		
		ОшибкиИдентификатора = ИТК_Строки.ПроверкаИдентификатора(Идентификатор, ЗанятыеИдентификаторыМассив, "_#");
		
	ИначеЕсли Режим = ИТК_Перечисления.РежимРедактированияИдентификатораСтрока() Тогда
		
		ОшибкиИдентификатора = ИТК_Строки.ПроверкаИдентификатораСтрока(Идентификатор, ЗанятыеИдентификаторыМассив);
		
	Иначе
		
		ВызватьИсключение НСтр("ru = 'Непредвиденное исключение';
								|en = 'Unexpected exception'");
		
	КонецЕсли;
	
	Для Каждого ОшибкаИдентификатора Из ОшибкиИдентификатора Цикл
		
		Результат.Добавить("Идентификатор", ОшибкаИдентификатора);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти