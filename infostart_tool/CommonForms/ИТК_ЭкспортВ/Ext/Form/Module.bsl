#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Заголовок = НСтр("ru = 'Экспорт в';
					|en = 'Export to'");
	ИТК_ПроверкаОшибокВФорме.Инициализировать(ЭтотОбъект);
	
	Имя = ИмяФайлаВыводаРезультата();
	
	СписокФорматов = Новый СписокЗначений;
	СписокФорматов.Добавить("XLSX", "Excel 2007", , БиблиотекаКартинок.ИТК_Excel);
	СписокФорматов.Добавить("MXL", "MXL");
	СписокФорматов.Добавить("PDF", "PDF");
	
	Если Не ЗначениеЗаполнено(Формат) Тогда
		Формат = "XLSX";
	КонецЕсли;
	
	УдалитьЗаголовок = Истина;
	
	Для Каждого ЭлементФормат Из СписокФорматов Цикл 
		
		ИмяКоманды = ЭлементФормат.Значение;
		
		ДополнительныеПараметры = Новый Структура;
		ДополнительныеПараметры.Вставить("Картинка", ЭлементФормат.Картинка);
		ИТК_Форма.ДобавитьКоманду(ЭтотОбъект, ИмяКоманды, "ПодключаемаяКомандаФормат", ЭлементФормат.Представление, ДополнительныеПараметры);
		
		Поля = Новый Структура;
		Поля.Вставить("ИмяКоманды", ИмяКоманды);
		Поля.Вставить("Отображение", ОтображениеКнопки.КартинкаИТекст);
		
		ИТК_Форма.ДобавитьЭлемент(ЭтотОбъект, "Кнопка" + ИмяКоманды, "Кнопка", Поля, Элементы.Формат);
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбновитьЭлементыФормы();
	
КонецПроцедуры

&НаСервере
Процедура ПередЗагрузкойДанныхИзНастроекНаСервере(Настройки)
	
	Если Не ЗначениеЗаполнено(Настройки["Формат"]) Тогда
		Настройки.Вставить("Формат", "XLSX");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Настройки["Имя"]) Тогда
		Настройки.Вставить("Имя", ИмяФайлаВыводаРезультата());
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура КаталогНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	ДиалогВыбораКаталога = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога);
	ДиалогВыбораКаталога.Каталог = Каталог;
	
	ДиалогВыбораКаталога.Показать(Новый ОписаниеОповещения("ЗавершенВыборКаталога", ЭтотОбъект));
	
КонецПроцедуры

&НаКлиенте
Процедура ИмяПриИзменении(Элемент)
	
	ПодключитьОбработчикОжидания("ПриИзмененииДанныхПослеОжидания", 0.1, Истина);	
	
КонецПроцедуры

&НаКлиенте
Процедура КаталогПриИзменении(Элемент = Неопределено)
	
	ПодключитьОбработчикОжидания("ПриИзмененииДанныхПослеОжидания", 0.1, Истина);	
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОшибкиОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	
	ИТК_ПроверкаОшибокВФормеКлиент.ОбработкаНавигационнойСсылки(ЭтотОбъект, Элемент, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПодключаемаяКомандаФормат(Команда)
	
	Формат = Команда.Имя;
	ОбновитьЭлементыФормы();
	
КонецПроцедуры

&НаКлиенте
Процедура ЭкспортВ(Команда)
	
	ОшибкиЗаполнения = ПроверкаЗаполнения();
	Если ЗначениеЗаполнено(ОшибкиЗаполнения) Тогда
		
		ИТК_ПроверкаОшибокВФормеКлиент.ТекущийЭлементПоПервой(ЭтотОбъект, ОшибкиЗаполнения);
		Возврат;
		
	КонецЕсли;
		
	Результат = Новый Структура;
	Результат.Вставить("ИмяФайла", СтрШаблон("%1\%2.%3", Каталог, Имя, РасширениеПоТипу(Формат)));
	Результат.Вставить("Формат", Формат);
	Результат.Вставить("ОткрытьПослеСохранения", ОткрытьПослеСохранения);
	Результат.Вставить("УдалитьЗаголовок", УдалитьЗаголовок);
	
	Закрыть(Результат);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиентеНаСервереБезКонтекста
Функция РасширениеПоТипу(ИмяФормата)
	
	Соответствие = Новый Соответствие;
	Соответствие.Вставить("XLS", "xls");
	Соответствие.Вставить("XLSX", "xlsx");
	Соответствие.Вставить("MXL", "mxl");
	Соответствие.Вставить("PDF", "pdf");
	Соответствие.Вставить("DOCX", "docx");
	Соответствие.Вставить("HTML5", "html");
	Соответствие.Вставить("ODS", "ods");
	
	Возврат Соответствие.Получить(ИмяФормата);
	
КонецФункции

&НаКлиенте
Процедура ЗавершенВыборКаталога(Результат, ДополнительныеПараметры) Экспорт
	
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Каталог = Результат[0];
	КаталогПриИзменении();
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьЭлементыФормы()
	
	Для Каждого КнопкаФормат Из Элементы.Формат.ПодчиненныеЭлементы Цикл 
		КнопкаФормат.Пометка = (Формат = КнопкаФормат.ИмяКоманды);
	КонецЦикла;
	
	Расширение = "." + РасширениеПоТипу(Формат);
	
	ИТК_ПроверкаОшибокВФормеКлиент.Обновить(ЭтотОбъект, ПроверкаЗаполнения());
	
КонецПроцедуры

&НаКлиенте
Процедура ПриИзмененииДанныхПослеОжидания()

	ОбновитьЭлементыФормы();

КонецПроцедуры

&НаКлиенте
Функция ПроверкаЗаполнения()
	
	Результат = Новый СписокЗначений;
	
	Если Не ЗначениеЗаполнено(Имя) Тогда
		Результат.Добавить("Имя", НСтр("ru = 'Не заполнено имя';
										|en = 'Name is not filled'"));
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Каталог) Тогда
		Результат.Добавить("Каталог", НСтр("ru = 'Не указан каталог';
											|en = 'Directory not specified'"));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ИмяФайлаВыводаРезультата()
	
	Возврат НСтр("ru = 'Результат';
				|en = 'Result'");
	
КонецФункции

#КонецОбласти
