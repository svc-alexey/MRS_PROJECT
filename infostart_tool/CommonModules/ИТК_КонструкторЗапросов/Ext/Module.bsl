#Область ПрограммныйИнтерфейс

// Задача 1
Функция МодульОбъектаFillSourcesItemsИначе(Source, Items) Экспорт
	
	Если ТипЗнч(Source) = Тип("ФиксированныйМассив") Then
		
		Items = Новый Массив;
		Для Индекс = 0 По Source.Количество() - 1 Цикл 
			
			ОписаниеВнешнейТаблицы = Source[Индекс];
			
			Если ОписаниеВнешнейТаблицы.Значение = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЭтоАдресВременногоХранилища(ОписаниеВнешнейТаблицы.Значение) Тогда
				ЗначениеВременнойТаблицы = ПолучитьИзВременногоХранилища(ОписаниеВнешнейТаблицы.Значение);
			Иначе
				ЗначениеВременнойТаблицы = ОписаниеВнешнейТаблицы.Значение;
			КонецЕсли;
			Items.Добавить(Новый Структура("Индекс, Ключ, Значение", Индекс, ОписаниеВнешнейТаблицы.Ключ, ЗначениеВременнойТаблицы));
			
		КонецЦикла;
		
		Результат = Истина;
		
	Иначе
		
		
		Результат = Ложь;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Задача 1, 2
Процедура МодульОбъектаПослеAddSourceItem(Source, ItemsTree) Экспорт
	
	// Задача 1
	Если ТипЗнч(Source) = Тип("Структура") Тогда
	
		СтрокаВременнаяТаблица = ItemsTree.Add();
		СтрокаВременнаяТаблица["Type"] = 1;
		СтрокаВременнаяТаблица["Name"] = Source.Ключ;
		СтрокаВременнаяТаблица["Presentation"] = Source.Ключ;
		СтрокаВременнаяТаблица["Index"] = Source.Индекс;
		СтрокаВременнаяТаблица["Picture"] = 17;
		
		// Поля таблицы
		КолонкиВременнойТаблицы = Source.Значение.Колонки;
		Для Индекс = 0 По КолонкиВременнойТаблицы.Количество() - 1 Цикл
			
			ПолеВременнойТаблицы = КолонкиВременнойТаблицы[Индекс];
			
			СтрокаПоле = СтрокаВременнаяТаблица.GetItems().Add();
			СтрокаПоле["Type"] = 2;
			СтрокаПоле["Presentation"] = ПолеВременнойТаблицы.Имя;
			СтрокаПоле["Name"] = СтрШаблон("%1.%2", Source.Ключ, ПолеВременнойТаблицы.Имя);
			СтрокаПоле["ValueType"] = ПолеВременнойТаблицы.ТипЗначения;
			СтрокаПоле["Index"] = Индекс;
			СтрокаПоле["Picture"] = 22;
			
			// Задача 2
			СтрокаПоле[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипПоля()] = ИТК_Типы.КартинкаОписания(ПолеВременнойТаблицы.ТипЗначения);
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

// Задача 1
Функция МодульОбъектаGetSourceВНачалеЦикла(Форма, Source, Indexes, Pos) Экспорт
	
	Если Indexes[Pos] = -1 Тогда
		
		Source = Форма[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников()];
		Результат = Истина;
		
	Иначе
		
		Результат = Ложь;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Задача 1, 3, 6, 7, 15, 17, 20, 21, 22, 26, 38
Процедура ФормаОсновнаяПриСозданииНаСервереПередЗаполнением(Форма, СхемаЗапроса) Экспорт
	
	Элементы = Форма.Элементы;
	ИдентификаторВнешнихИсточников = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников();
	
	Форма.Автозаголовок = Ложь;
	Форма.АвтоНавигационнаяСсылка = Ложь;
	Форма.Заголовок = НСтр("ru = 'Конструктор запроса';
							|en = 'Query constructor'");
	
	ДобавитьКолонкуТипПоля(Форма, "AvailableTables");
	ДобавитьКолонкуТипПоля(Форма, "Sources");
	ДобавитьКолонкуТипПоля(Форма, "AllFieldsForGrouping");
	ДобавитьКолонкуТипПоля(Форма, "AllFieldsForConditions");
	ДобавитьКолонкуТипПоля(Форма, "Aliases");
	ДобавитьКолонкуТипПоля(Форма, "AllFieldsForOrder");
	ДобавитьКолонкуТипПоля(Форма, "AllFieldsForIndex");
	ДобавитьКолонкуТипПоля(Форма, "AllFieldsForTotals");
	
	ДобавитьКолонкуТипЗначенияПоля(Форма, "Aliases");

	Параметры = Форма.Параметры;
	
	Если Параметры.Свойство("IsNestedQuery") Тогда // Не вложенный запрос
		
		Форма.Object[ИдентификаторВнешнихИсточников] = ИТК_ОбщийКлиентСервер.Свойство(Параметры, ИдентификаторВнешнихИсточников);
		
	Иначе
		
		// Задача 1
		// Инициализация внешних источников
		TempTablesNames = Форма.TempTablesNames;
		
		ЗначенияВнешнихИсточников = Новый Массив;
		Если Параметры.Свойство("Данные") Тогда
			
			Данные = Параметры.Данные;
			
			Для Каждого ОписаниеВнешнегоИсточника Из ИТК_КонсольРазработчика.ЗначенияВнешнихИсточников(Данные) Цикл 
				
				Если ОписаниеВнешнегоИсточника.Значение = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				Если ЭтоАдресВременногоХранилища(ОписаниеВнешнегоИсточника.Значение) Тогда
					ТаблицаВнешнегоИсточника = ПолучитьИзВременногоХранилища(ОписаниеВнешнегоИсточника.Значение);
					АдресВнешнегоИсточника = ОписаниеВнешнегоИсточника.Значение;
				Иначе 
					ТаблицаВнешнегоИсточника = ОписаниеВнешнегоИсточника.Значение;
					АдресВнешнегоИсточника = ПоместитьВоВременноеХранилище(ТаблицаВнешнегоИсточника, Форма.УникальныйИдентификатор);
				КонецЕсли;
				
				Если ТаблицаВнешнегоИсточника = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				Если ЗначениеЗаполнено(ТаблицаВнешнегоИсточника.Колонки) Тогда
					
					ЗначенияВнешнихИсточников.Добавить(Новый Структура("Ключ, Значение", ОписаниеВнешнегоИсточника.Ключ, АдресВнешнегоИсточника));
					
					НоваяСтрока = TempTablesNames.Добавить();
					НоваяСтрока.Name = ОписаниеВнешнегоИсточника.Ключ;
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
		Форма.Object[ИдентификаторВнешнихИсточников] = Новый ФиксированныйМассив(ЗначенияВнешнихИсточников);
		
		// Переинициализация используемых внешних источников
		Для Каждого ПакетСхемы Из СхемаЗапроса.ПакетЗапросов Цикл 
			
			Если ТипЗнч(ПакетСхемы) = Тип("ЗапросУничтоженияТаблицыСхемыЗапроса") Тогда
				Продолжить;
			КонецЕсли;
			
			Для Каждого ОператорСхемы Из ПакетСхемы.Операторы Цикл 
				
				Для Каждого ИсточникОператораСхемы Из ОператорСхемы.Источники Цикл 
					ДополнитьДанныеИсточниковВходнымиВнешнимиИсточниками(ОператорСхемы.Источники, ИсточникОператораСхемы, ЗначенияВнешнихИсточников);
				КонецЦикла;
				
			КонецЦикла;
			
		КонецЦикла;
		
		// Задача 3
		Форма.CurrentQuerySchemaSelectQuery = СхемаЗапроса.ПакетЗапросов.Количество() - 1; // текущий запрос последний
		
	КонецЕсли;
	
	// Задача 6
	Элементы["GroupingFields"].ИзменятьСоставСтрок = Истина;
	Элементы["GroupingFieldsPresentation"].ТолькоПросмотр = Истина;
	
	// Задача 7
	Элементы["AdditionallyTablesForChanging"].ТолькоПросмотр = Ложь;
	Элементы["AdditionallyTablesForChangingPresentation"].ТолькоПросмотр = Истина;
	
	// Задача 16
	Элементы["Indexes"].ТолькоПросмотр = Ложь;
	
	// Задача 15
	ПутьКДаннымЗаголовкаТаблицыПоля = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаТаблицыПоля();
	ПутьКДаннымЗаголовкаСвязи = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаСвязи();
	ПутьКДаннымЗаголовкаГруппировка = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаГруппировка();
	ПутьКДаннымЗаголовкаУсловия = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаУсловия();
	ПутьКДаннымЗаголовкаДополнительно = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаДополнительно();
	ПутьКДаннымЗаголовкаОбъединить = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаОбъединить();
	ПутьКДаннымЗаголовкаИндекс = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаИндекс();
	ПутьКДаннымЗаголовкаПорядок = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаПорядок();
	ПутьКДаннымЗаголовкаИтоги = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаИтоги();
	ПутьКДаннымЗаголовкаПакетЗапросов = ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаПакетЗапросов();
	
	ДобавляемыеРеквизиты = Новый Массив;
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаТаблицыПоля, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаСвязи, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаГруппировка, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаУсловия, "Число");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаДополнительно, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаОбъединить, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаИндекс, "Число");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаПорядок, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаИтоги, "Строка(1)");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ПутьКДаннымЗаголовкаПакетЗапросов, "Число");
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	Элементы.TablesAndFieldsPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаТаблицыПоля;
	Элементы.JoinsPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаСвязи;
	Элементы.GroupingPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаГруппировка;
	Элементы.ConditionsPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаУсловия;
	Элементы.AdditionallyPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаДополнительно;
	Элементы.UnionsAliasesPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаОбъединить;
	Элементы.IndexPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаИндекс;
	Элементы.OrderPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаПорядок;
	Элементы.TotalsPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаИтоги;
	Элементы.QueryBatchPage.ПутьКДаннымЗаголовка = ПутьКДаннымЗаголовкаПакетЗапросов;
	
	ОбновитьЗаголовкиСтраниц(Форма);
	
	// Задача 26
	ДобавляемыеРеквизиты = Новый Массив;
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымСлужебныйЗапуск(), "Булево");
	// Задача 41
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИТК_КонструкторЗапросовКлиентСервер.ИмяТаблицыПослеРазворачивания(), "Строка");
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	// Задача 17
	ДобавитьКомандыЭлементыПорядокУсловий(Форма);
	
	// Задача 20
	ДобавитьРеквизитыЭлементыСвязей(Форма);
	
	// Задача 21
	ДобавитьКомандуПерейтиКПолюВВыбранных(Форма);
	
	// Задача 22
	ЭлементУсловие = Элементы["ConditionsCondition"];
	ЭлементУсловие.МногострочныйРежим = Истина;
	ЭлементУсловие.АвтоВысотаЯчейки = Истина;
	ЭлементУсловие.Высота = 5;
	
	// Задача 38
	Инструмент = ИТК_Перечисления.ИнструментМетаданные();
	Если ИТК_ТуллкитКлиентСервер.ДоступенИнструмент(Инструмент) Тогда
	
		ДобавитьКомандуМетаданные(Форма, "AvailableTables");
		ДобавитьКомандуМетаданные(Форма, "Sources");
		ДобавитьКомандуМетаданные(Форма, "AllFieldsForGrouping");
		ДобавитьКомандуМетаданные(Форма, "AllFieldsForConditions");
		
		ДобавитьКомандуМетаданные(Форма, "AdditionallyTables");
		ДобавитьКомандуМетаданные(Форма, "AdditionallyTablesForChanging");
		
		ДобавитьКомандуМетаданные(Форма, "AllFieldsForIndex");
		ДобавитьКомандуМетаданные(Форма, "AllFieldsForOrder");
		ДобавитьКомандуМетаданные(Форма, "AllFieldsForTotals");
	
	КонецЕсли;
	
	// Задача 40
	ФормаОсновнаяПереводНаАнглийский(Форма);
	
	// Задача 41
	ИмяЭлемента = "AvailableTables";
	
	Префикс = ИТК_КонструкторЗапросовКлиентСервер.Префикс();
	ИмяКоманды = Префикс + "_ПоискТаблицы_" + ИмяЭлемента;
	ЗаголовокКоманды = НСтр("ru = 'Поиск таблицы...'; en = 'Search table...'");
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Картинка", ИТК_ТуллкитКлиентСервер.КартинкаИнструмента(Инструмент));
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Картинка", БиблиотекаКартинок.ИТК_Найти);

	ИмяДействия = ИТК_КонструкторЗапросовКлиентСервер.ОсновноеИмяДействия();
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, ЗаголовокКоманды, ДополнительныеПараметры);
											
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды + "КонтекстноеМеню", "Кнопка", Поля, Форма.Элементы[ИмяЭлемента].КонтекстноеМеню);
	
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды + "КоманднаяПанель", "Кнопка", Поля, Форма.Элементы[ИмяЭлемента].КоманднаяПанель);
	
	// Задача ?
	Элементы.CurrentQuerySchemaSelectQuery.Заголовок = НСтр("ru = 'Запрос'; en = 'Query'");
	Элементы.CurrentQuerySchemaOperator.Заголовок = НСтр("ru = 'Оператор'; en = 'Operator'");
  
КонецПроцедуры

// Задача 1, 15, 20
Процедура ФормаОсновнаяПослеFillPagesAtServer(Форма) Экспорт
	
	// Задача 15
	ОбновитьЗаголовкиСтраниц(Форма);
	
	// Задача 1
	ЗначенияВнешнихИсточников = ВнешниеИсточники(Форма);
	Если ЗначениеЗаполнено(ЗначенияВнешнихИсточников) Тогда
		
		NameВременныеТаблицы = NameВременныеТаблицыВходные();
		СтрокаВременныеТаблицы = Неопределено;
		Для Каждого СтрокаДоступныйИсточник Из Форма.AvailableTables.ПолучитьЭлементы() Цикл 
			
			Если СтрокаДоступныйИсточник.Name = NameВременныеТаблицы Тогда
				СтрокаВременныеТаблицы = СтрокаДоступныйИсточник;
				Прервать;
			КонецЕсли;
			
		КонецЦикла;
		
		Если СтрокаВременныеТаблицы = Неопределено Тогда
			
			AvailableTables = Форма.AvailableTables;
			ЭлементыДоступныеТаблицы = AvailableTables.ПолучитьЭлементы();
			
			ИндексВставки = Неопределено;
			Для Каждого Строка Из ЭлементыДоступныеТаблицы Цикл 
				
				Если Строка.Name = "Временные таблицы" Тогда
					
					ИндексВставки = Строка.Index;
					Прервать;
					
				КонецЕсли;
				
			КонецЦикла;
			
			// Новая строка (когда есть временные вставляем перед ней
			Если ИндексВставки = Неопределено Тогда
				СтрокаВременныеТаблицы = ЭлементыДоступныеТаблицы.Добавить();
			Иначе
				СтрокаВременныеТаблицы = ЭлементыДоступныеТаблицы.Вставить(ИндексВставки);
			КонецЕсли;
			СтрокаВременныеТаблицы.Type = 1;
			СтрокаВременныеТаблицы.Name = NameВременныеТаблицы;
			СтрокаВременныеТаблицы.Presentation = NameВременныеТаблицы;
			СтрокаВременныеТаблицы.Picture = 20;
			СтрокаВременныеТаблицы.Index = -1;
			
			ПустаяСтрока = СтрокаВременныеТаблицы.ПолучитьЭлементы().Добавить();
			ПустаяСтрока.Name = "FakeFieldeItem";
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Задача 20
	Попытка
		
		ЗаполнениеСвязей(Форма);
		
	Исключение

		ШаблонТекста = НСтр("ru = 'Ошибка при заполнении связей:
                            |%1';
                            |en = 'Error filling links:
                            |%1'");
		КраткоеПредставлениеОшибки = КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
		Текст = СтрШаблон(ШаблонТекста, КраткоеПредставлениеОшибки);
		
		Сообщить(Текст, СтатусСообщения.ОченьВажное);
		
	КонецПопытки;

КонецПроцедуры

// Задача 1
Процедура ФормаОсновнаяВнутриApplyChangesFromCacheNoContextДобавитьВременнуюТаблицуВИсточникиОператора(ВнешниеИсточники, Source, 
																										Operator, ItemIndexes, Expression = Неопределено) Экспорт
	Если ItemIndexes[0] = -1 Тогда
		
		ОписаниеВременнойТаблицы = ВнешниеИсточники[ItemIndexes[1]];
		
		ИсточникВременнаяТаблица = Operator.Источники.НайтиПоИмени(ОписаниеВременнойТаблицы.Ключ);
		Если ИсточникВременнаяТаблица = Неопределено Тогда
			
			ИсточникВременнаяТаблица = Operator.Источники.Добавить(Тип("ОписаниеВременнойТаблицыСхемыЗапроса"), ОписаниеВременнойТаблицы.Ключ);
			
			Если ЭтоАдресВременногоХранилища(ОписаниеВременнойТаблицы.Значение) Тогда
				ЗначениеВременнойТаблицы = ПолучитьИзВременногоХранилища(ОписаниеВременнойТаблицы.Значение);
			Иначе 
				ЗначениеВременнойТаблицы = ОписаниеВременнойТаблицы.Значение;
			КонецЕсли;
			
			Для Каждого ПолеВременнойТаблицы Из ЗначениеВременнойТаблицы.Колонки Цикл 
				
				ТипЗначения = ПолеВременнойТаблицы.ТипЗначения;
				ДоступныеПоля = ИсточникВременнаяТаблица.Источник.ДоступныеПоля;
				
				// Видимо ошибка, не воспринимает составные типы
				Если ИТК_ТипыКлиентСервер.Количество(ТипЗначения) = 1 Тогда
					
					ПервыйТип = ТипЗначения.Типы()[0];
					ДоступныеПоля.Добавить(ПолеВременнойТаблицы.Имя, ПервыйТип);
					
				Иначе
					
					ДоступныеПоля.Добавить(ПолеВременнойТаблицы.Имя);
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
		Source = ИсточникВременнаяТаблица;
		
		Если ItemIndexes.Количество() > 2 Тогда
			Expression = СтрШаблон("%1.%2", Source.Источник.ИмяТаблицы, Source.Источник.ДоступныеПоля[ItemIndexes[2]].Имя);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Задача 1
Функция ВнешниеИсточники(Форма) Экспорт
	
	Возврат Форма.Object[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников()];
	
КонецФункции

// Задача 1, 2, 27, 38
Процедура ФормаРедактированиеПоляПриСозданииНаСервереПередЗаполнением(Форма) Экспорт
	
	Элементы = Форма.Элементы;
	
	// Задача 1
	ИдентификаторВнешнихИсточников = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников();
	Форма.Object[ИдентификаторВнешнихИсточников] = Форма.Параметры[ИдентификаторВнешнихИсточников];
	
	// Задача 2
	ДобавитьКолонкуТипПоля(Форма, "Fields");
	
	// Задача 38
	ДобавитьКомандуМетаданные(Форма, "Fields");
	
	// Задача 27
	ДополнительныеСвойства = Новый Структура;
	ДополнительныеСвойства.Вставить("Текст", Форма["Expression"]);
	ДополнительныеСвойства.Вставить("ТипЯзыка", ИТК_Перечисления.ТипЯзыкаРедактораЗапросы());
	
	ИспользованиеКоманд = Новый Структура;
	ИспользованиеКоманд.Вставить("КонструкторВыделенного", Ложь);
	ИспользованиеКоманд.Вставить("ПреобразоватьВоВложенный", Ложь);
	ИспользованиеКоманд.Вставить("УбратьПереносы", Ложь);
	ИспользованиеКоманд.Вставить("ПерейтиКСтроке", Ложь);
	ДополнительныеСвойства.Вставить("ИспользованиеКоманд", ИспользованиеКоманд);
	
	ФиксированныеНастройки = ИТК_РедакторКодаКлиентСервер.ФиксированныеНастройкиВыражения();
	ДополнительныеСвойства.Вставить("ФиксированныеНастройки", ФиксированныеНастройки);
	
	ИТК_РедакторКода.Инициализация(Форма, "Выражение", ДополнительныеСвойства);

КонецПроцедуры

// Задача 1
Процедура ФормаРедактированиеПараметровВиртуальныхТаблицПриСозданииНаСервере(Форма) Экспорт
	
	ИдентификаторВнешнихИсточников = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников();
	Форма.Object[ИдентификаторВнешнихИсточников] = Форма.Параметры[ИдентификаторВнешнихИсточников];

КонецПроцедуры

// Задача 1, 2
Процедура ФормаВыбораТаблицыПриСозданииНаСервере(Форма) Экспорт
	
	// Задача 1
	ИдентификаторВнешнихИсточников = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников();
	Форма.Object[ИдентификаторВнешнихИсточников] = Форма.Параметры[ИдентификаторВнешнихИсточников];
	
	// Удалим строку временные таблицы (входные)
	ДеревоДоступныеТаблицы = Форма["AvailableTables"];
	ДеревоДоступныеТаблицыЭлементы = ДеревоДоступныеТаблицы.ПолучитьЭлементы();
	Для Каждого Строка Из ДеревоДоступныеТаблицыЭлементы Цикл 
		
		Если Строка.Name = NameВременныеТаблицыВходные() Тогда
			
			ДеревоДоступныеТаблицыЭлементы.Удалить(Строка);
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	// Задача 2
	ДобавитьКолонкуТипПоля(Форма, "AvailableTables");

КонецПроцедуры

// Задача 2
Процедура МодульОбъектаAddSourceItemИначеЕслиAvailableField(Source, NewElement) Экспорт
	
	NewElement[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипПоля()] = ИТК_Типы.КартинкаОписания(Source.ValueType);
	
КонецПроцедуры

// Задача 3, 23, 24
Процедура ФормаОсновнаяПриСозданииНаСервереПослеЗаполнения(Форма, Отказ) Экспорт
	
	Параметры = Форма.Параметры;
	
	СлужебныйЗапуск = Параметры.Свойство("IsNestedQuery") ИЛИ Параметры.Свойство("QueryText");
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымСлужебныйЗапуск()] = СлужебныйЗапуск;
	
	УсловноеОформление = Форма.УсловноеОформление;
	
	// 3
	ИмяТаблицы = "QueryBatch";
	ЗначениеРеквизита = Форма[ИмяТаблицы];
	
	Элементы = Форма.Элементы;
	Элементы[ИмяТаблицы].ТекущаяСтрока = ЗначениеРеквизита[ЗначениеРеквизита.Количество() - 1].ПолучитьИдентификатор();
	
	// 23
	ОформлениеПоля = Новый Структура;
	ОформлениеПоля.Вставить("Шрифт", ИТК_ОбщийКлиентСервер.ШрифтЖирный());

	ИТК_Форма.ДобавитьУсловноеОформление(УсловноеОформление, "QueryBatchName", ОформлениеПоля, "QueryBatch.Index", Новый ПолеКомпоновкиДанных("CurrentQuerySchemaSelectQuery"));
	ИТК_Форма.ДобавитьУсловноеОформление(УсловноеОформление, "UnionsName", ОформлениеПоля, "Unions.Index", Новый ПолеКомпоновкиДанных("CurrentQuerySchemaOperator"));
	
	// 24 
	ИмяДействия = ИТК_КонструкторЗапросовКлиентСервер.ОсновноеИмяДействия();
	Префикс = ИТК_ТуллкитКлиентСервер.Префикс();

	Элементы["CurrentQuerySchemaSelectQuery"].Видимость = Истина;
	Элементы["CurrentQuerySchemaOperator"].Видимость = Истина;
	Элементы["ShowQuery"].Видимость = Ложь;
	
	// Добавление кнопки запрос
	ИмяКоманды = "ShowQuery";
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, Префикс + ИмяКоманды, "Кнопка", Поля, Элементы["Commands"], Элементы["CurrentQuerySchemaSelectQuery"]);
	
	// Добавление кнопок Назад и Далее
	// < Назад
	ИмяКоманды = Префикс + "ЗапросПредыдущаяСтраница";
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, НСтр("ru = '< Назад ';
																	|en = '< Back'"));
											
	Поля = Новый Структура;
	Поля.Вставить("ИмяКоманды", ИмяКоманды);
	Поля.Вставить("Доступность", Ложь);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды, "Кнопка", Поля, Элементы["Group1"], Элементы["ShowQuery"]);
	
	// Далее
	ИмяКоманды = Префикс + "ЗапросСледующаяСтраница";
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, НСтр("ru = 'Далее >';
																	|en = 'Next >'"));
											
	Поля = Новый Структура;
	Поля.Вставить("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды, "Кнопка", Поля, Элементы["Group1"], Элементы["ShowQuery"]);
	
	// 25
	Элементы["AvailableFields"].ЧередованиеЦветовСтрок = Истина;
	Элементы["Conditions"].ЧередованиеЦветовСтрок = Истина;
	Элементы["Aliases"].ЧередованиеЦветовСтрок = Истина;
	Элементы["Indexes"].ЧередованиеЦветовСтрок = Истина;
	Элементы["Order"].ЧередованиеЦветовСтрок = Истина;
	Элементы["AllFieldsForTotals"].ЧередованиеЦветовСтрок = Истина;
	Элементы["TotalsGroupingFields"].ЧередованиеЦветовСтрок = Истина;
	Элементы["TotalsExpressions"].ЧередованиеЦветовСтрок = Истина;
	Элементы["QueryBatch"].ЧередованиеЦветовСтрок = Истина;
	
КонецПроцедуры

// Задача 10, 12, 27
Процедура ФормаРедактированиеТекстаПослеПриСозданииНаСервере(Форма) Экспорт
	
	Элементы = Форма.Элементы;
	
	// Задача 1
	ИдентификаторВнешнихИсточников = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников();
	Форма.Object[ИдентификаторВнешнихИсточников] = Форма.Параметры[ИдентификаторВнешнихИсточников];
	
	// Задача 10
	Форма.Ширина = 120;
	Форма.Высота = 40;
	
	// Задача 12, 27
	ДополнительныеСвойства = Новый Структура;
	ДополнительныеСвойства.Вставить("Текст", Форма["QueryText"]);
	ДополнительныеСвойства.Вставить("ТипЯзыка", ИТК_Перечисления.ТипЯзыкаРедактораЗапросы());
	
	ИспользованиеКоманд = Новый Структура;
	ИспользованиеКоманд.Вставить("Конструктор", Ложь);
	ДополнительныеСвойства.Вставить("ИспользованиеКоманд", ИспользованиеКоманд);

	ИТК_РедакторКода.Инициализация(Форма, "ТекстЗапроса", ДополнительныеСвойства);
	
КонецПроцедуры

Функция КартинкаИсточникаСвязи(Источник) Экспорт
	
	Если ТипЗнч(Источник) = Тип("ТаблицаСхемыЗапроса") Тогда
		Результат = КартинкаИсточникаСвязи(Источник.ИмяТаблицы);
	ИначеЕсли ТипЗнч(Источник) = Тип("ВложенныйЗапросСхемыЗапроса") Тогда
		Результат = БиблиотекаКартинок.КонструкторЗапросаВложенныйЗапрос
	Иначе
		
		Если СтрНайти(Источник, ".") Тогда
			
			Попытка
				
				Если СтрЗаканчиваетсяНа(Источник, ".Изменения") Тогда
					Результат = БиблиотекаКартинок.ИТК_ИзмененияОбъекта;
				Иначе
					Результат = БиблиотекаКартинок[ИТК_Строки.РазборПрочитатьИдентификатор(Источник)];
				КонецЕсли;
				
			Исключение
				Результат = Новый Картинка;
			КонецПопытки;
			
		Иначе
			Результат = БиблиотекаКартинок.КонструкторЗапросаВременнаяТаблица;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПользовательскиеОбъектыПодсказки(Форма) Экспорт
	
	МодульОбъекта = Форма.РеквизитФормыВЗначение("Object");
	РедактированиеВыражения = Булево(СтрНайти(Форма.ИмяФормы, "ArbitraryExpression"));
	
	Результат = ИТК_РедакторКода.НовыеПользовательскиеОбъекты();
	Кэш = ИТК_РедакторКода.КэшТиповПользовательскихОбъектов();
	
	// Внешние источники
	ВнешниеИсточники = МодульОбъекта[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторЗначенийВнешнихИсточников()];
	Для Каждого ОписаниеВнешнегоИсточника Из ВнешниеИсточники Цикл 
		
		ВнешнийИсточник = ПолучитьИзВременногоХранилища(ОписаниеВнешнегоИсточника.Значение);
		ИТК_РедакторКода.ЗагрузитьПользовательскиеДанныеПоВнешнемуИсточнику(Результат, Кэш, ОписаниеВнешнегоИсточника.Ключ, ВнешнийИсточник);

	КонецЦикла;
	
	Если РедактированиеВыражения Тогда
		
		СхемаЗапроса = ПолучитьИзВременногоХранилища(Форма["QueryWizardAddress"]);
		
		// Параметры
		Параметры = СхемаЗапроса.НайтиПараметры();
		Для Каждого Параметр Из Параметры Цикл 
			ИТК_РедакторКода.ДобавитьПользовательскийОбъект(Результат, "&" + Параметр.Имя);
		КонецЦикла;
		
		ЗапросСхемы = МодульОбъекта.GetSchemaQuery(СхемаЗапроса, Форма["CurrentQuerySchemaSelectQuery"], Форма["NestedQueryPositionAddress"]);
		ОператорСхемы = ЗапросСхемы.Операторы[Форма["CurrentQuerySchemaOperator"]];
		
		// Источники
		ИмяПараметраВиртуальнойТаблицы = Форма["PropertyName"];
		Если ЗначениеЗаполнено(ИмяПараметраВиртуальнойТаблицы) Тогда
			
			ИсточникВиртуальнаяТаблица = ОператорСхемы.Источники[Форма["TableIndex"]];
			СобратьДанныеИсточников(ИсточникВиртуальнаяТаблица, Результат, Кэш, ИмяПараметраВиртуальнойТаблицы);
			
		Иначе
			
			Для Каждого Источник Из ОператорСхемы.Источники Цикл 
				СобратьДанныеИсточников(Источник, Результат, Кэш);
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Задача 30
Процедура ОбновитьПоляВременныхТаблиц(Форма, СхемаЗапроса, ОператорСхемы) Экспорт
	
	Для Каждого ИсточникСхемы Из ОператорСхемы.Источники Цикл 

		Источник = ИсточникСхемы.Источник;
		Если ТипЗнч(Источник) <> Тип("ТаблицаСхемыЗапроса")
				ИЛИ СтрНайти(Источник.ИмяТаблицы, ".") Тогда
			Возврат;
		КонецЕсли;
		
		Для Каждого ЗапросПакета Из СхемаЗапроса.ПакетЗапросов Цикл 
			
			Если ТипЗнч(ЗапросПакета) = Тип("ЗапросУничтоженияТаблицыСхемыЗапроса") Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЗапросПакета.ТаблицаДляПомещения <> Источник.ИмяТаблицы Тогда 
				Продолжить;
			КонецЕсли;
			
			Если ЗапросПакета.Колонки.Количество() <= Источник.ДоступныеПоля.Количество() Тогда
				Продолжить;
			КонецЕсли;
			
			// ДоступныеПоля изменить не получилось т.к. для таблицы только чтение
			// Пришлось переинициализировать схему запроса
			ТекстЗапроса = СхемаЗапроса.ПолучитьТекстЗапроса();
			
			НоваяСхемаЗапроса = Новый СхемаЗапроса;
			НоваяСхемаЗапроса.УстановитьТекстЗапроса(ТекстЗапроса);
			
			СхемаЗапроса = НоваяСхемаЗапроса;
			
			МодульОбъекта = Форма.РеквизитФормыВЗначение("Object");
			ЗапросСхемы = МодульОбъекта.GetSchemaQuery(СхемаЗапроса, Форма["CurrentQuerySchemaSelectQuery"], Форма["NestedQueryPositionAddress"]);
			ОператорСхемы = ЗапросСхемы.Операторы.Get(Форма["CurrentQuerySchemaOperator"]);
			ПоместитьВоВременноеХранилище(СхемаЗапроса, Форма["QueryWizardAddress"]);
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

// Задача 2, 33
Процедура ЗаполнитьДополнительныеПоляПолейОбъединения(Строка, Колонка) Экспорт
	
	Если ТипЗнч(Колонка) = Тип("КолонкаВложеннаяТаблицаСхемыЗапроса") Тогда
		Возврат;
	КонецЕсли;
	
	Строка[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипПоля()] = ИТК_Типы.КартинкаОписания(Колонка.ТипЗначения);
	Строка[ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипЗначенияПоля()] = Колонка.ТипЗначения;
	
КонецПроцедуры

Процедура ОбработкаЗаполненияПустыхПолейОбъединения(Форма, Параметры) Экспорт
	
	Форма.Модифицированность = Истина;
	
	МодульОбъекта = Форма.РеквизитФормыВЗначение("Object");
	СхемаЗапроса = ПолучитьИзВременногоХранилища(Форма["QueryWizardAddress"]);
	ЗапросСхемы = МодульОбъекта.GetSchemaQuery(СхемаЗапроса, Форма["CurrentQuerySchemaSelectQuery"], Форма["NestedQueryPositionAddress"]);
	
	ОператорСхемы = ЗапросСхемы.Операторы[Параметры.ИндексОператора];
	ОператорСхемы.ВыбираемыеПоля.Добавить(Параметры.Выражение, Параметры.ИндексКолонки);
	
КонецПроцедуры

Функция ПолноеИмяФормыОбработки() Экспорт
	
	Имя = ИТК_КонструкторЗапросовПовтИсп.ИмяОбработки();
	
	Если ЗначениеЗаполнено(Имя) Тогда
		
		Возврат СтрШаблон("Обработка.%1.Форма", Имя);
		
	Иначе
		
		Результат = Неопределено;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СписокВыбораИсточникаСвязей(Источники, ИмяИсключения) Экспорт
	
	Результат = Новый СписокЗначений;
	Для Каждого ОписаниеИсточника Из Источники.ПолучитьЭлементы() Цикл 
		
		Если ОписаниеИсточника.Name = ИмяИсключения Тогда
			Продолжить;
		КонецЕсли;
		
		Картинка = ИТК_КонструкторЗапросов.КартинкаИсточникаСвязи(ОписаниеИсточника.TableName);
		Результат.Добавить(ОписаниеИсточника.Name, ОписаниеИсточника.Presentation, , Картинка);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ДобавитьКолонкуТипПоля(Форма, ИмяКоллекции)
	
	ИмяРеквизитаКолонки = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипПоля();
	ЭлементРодитель = Форма.Элементы[ИмяКоллекции];
	
	// Добавляем реквизит и колонку типа поля
	ДобавляемыеРеквизиты = Новый Массив;
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИмяРеквизитаКолонки, "Картинка", , ИмяКоллекции);
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", СтрШаблон("%1.%2", ИмяКоллекции, ИмяРеквизитаКолонки));
	Поля.Вставить("КартинкаШапки", БиблиотекаКартинок.ИТК_ТипТип);
	Поля.Вставить("ПоложениеЗаголовка", ПоложениеЗаголовкаЭлементаФормы.Нет);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоллекции + ИмяРеквизитаКолонки, "ПолеКартинки", Поля, ЭлементРодитель);
	
	// Модифицируем элемент таблица
	ЭлементРодитель.ЧередованиеЦветовСтрок = Истина;
	ЭлементРодитель.РежимВыделенияСтроки = РежимВыделенияСтрокиТаблицы.Строка;
	
КонецПроцедуры

Процедура ДобавитьКолонкуТипЗначенияПоля(Форма, ИмяКоллекции)
	
	ИмяРеквизитаКолонки = ИТК_КонструкторЗапросовКлиентСервер.ИдентификаторКолонкиТипЗначенияПоля();
	ЭлементРодитель = Форма.Элементы[ИмяКоллекции];
	
	// Добавляем реквизит и колонку типа поля
	ДобавляемыеРеквизиты = Новый Массив;
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИмяРеквизитаКолонки, "ОписаниеТипов", , ИмяКоллекции);
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", СтрШаблон("%1.%2", ИмяКоллекции, ИмяРеквизитаКолонки));
	Поля.Вставить("Видимость", Ложь);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоллекции + ИмяРеквизитаКолонки, "ПолеВвода", Поля, ЭлементРодитель);
	
КонецПроцедуры

Процедура ОбновитьЗаголовкиСтраниц(Форма) Экспорт
	
	МодульОбъекта = Форма.РеквизитФормыВЗначение("Object");
	РежимКомпоновкиДанных = РежимКомпоновкиДанныхФормы(Форма);
		
	СхемаЗапроса = ПолучитьИзВременногоХранилища(Форма["QueryWizardAddress"]);
	ЗапросСхемы = МодульОбъекта.GetSchemaQuery(СхемаЗапроса, Форма["CurrentQuerySchemaSelectQuery"], Форма["NestedQueryPositionAddress"]);
	
	// Пакет запросов
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаПакетЗапросов()] = СхемаЗапроса.ПакетЗапросов.Количество();
	
	Если ТипЗнч(ЗапросСхемы) = Тип("ЗапросУничтоженияТаблицыСхемыЗапроса") Тогда
		Возврат;
	КонецЕсли;
	
	ОператорСхемы = ЗапросСхемы.Операторы[Форма.CurrentQuerySchemaOperator];
	
	// Таблицы и поля
	ИТК_КонструкторЗапросовКлиентСервер.ОбновитьЗаголовокСтраницыТаблицыПоля(Форма, ОператорСхемы.ВыбираемыеПоля);
	
	// Связи 
	ДанныеСвязей = ДанныеСвязей(ОператорСхемы.Источники);
	ИТК_КонструкторЗапросовКлиентСервер.ОбновитьЗаголовокСтраницыСвязи(Форма, ДанныеСвязей.ЕстьСвязи, ДанныеСвязей.Источников);
	
	// Группировка
	ЗаголовокГруппировка = "";
	Если ЗначениеЗаполнено(ОператорСхемы.Группировка) Тогда
			
		ЗаголовокГруппировка = "*";
		
	КонецЕсли;
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаГруппировка()] = ЗаголовокГруппировка;
	
	// Условия
	ИТК_КонструкторЗапросовКлиентСервер.ОбновитьЗаголовокСтраницыУсловия(Форма, ОператорСхемы.Отбор);
	
	// Дополнительно
	Параметры = Новый Структура;
	Параметры.Вставить("ВыбиратьРазрешенные", ЗапросСхемы.ВыбиратьРазрешенные);
	Параметры.Вставить("ВыбиратьРазличные", ОператорСхемы.ВыбиратьРазличные);
	Параметры.Вставить("ВыбиратьДляИзменения", ОператорСхемы.ВыбиратьДляИзменения);
	
	Параметры.Вставить("КоличествоПолучаемыхЗаписей", ОператорСхемы.КоличествоПолучаемыхЗаписей);
	Параметры.Вставить("ТаблицаДляПомещения", ЗапросСхемы.ТаблицаДляПомещения);
	Параметры.Вставить("ТаблицыДляИзменения", ОператорСхемы.ТаблицыДляИзменения);	

	ИТК_КонструкторЗапросовКлиентСервер.ОбновитьЗаголовокСтраницыДополнительно(Форма, Параметры);
	
	// Объединить
	ЗаголовокОбъединить = "";
	Если ЗапросСхемы.Операторы.Количество() > 1 Тогда
			
		ЗаголовокОбъединить = "*";
		
	КонецЕсли;
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаОбъединить()] = ЗаголовокОбъединить;
	
	// Индекс
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаИндекс()] = ЗапросСхемы.Индекс.Количество();
	
	// Порядок
	ЗаголовокПорядок = "";
	Если ЗапросСхемы.Автопорядок
			ИЛИ ЗначениеЗаполнено(ЗапросСхемы.Порядок) Тогда
			
		ЗаголовокПорядок = "*";
		
	КонецЕсли;
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаПорядок()] = ЗаголовокПорядок;
	
	Если РежимКомпоновкиДанных Тогда
		Возврат;
	КонецЕсли;
		
	// Итоги
	ЗаголовокИтоги = "";
	Если ЗапросСхемы.ОбщиеИтоги 
			ИЛИ ЗначениеЗаполнено(ЗапросСхемы.КонтрольныеТочкиИтогов)
			ИЛИ ЗначениеЗаполнено(ЗапросСхемы.ВыраженияИтогов) Тогда
			
		ЗаголовокИтоги = "*";
		
	КонецЕсли;
	Форма[ИТК_КонструкторЗапросовКлиентСервер.ПутьКДаннымЗаголовкаИтоги()] = ЗаголовокИтоги;
	
КонецПроцедуры

Процедура ДобавитьКомандыЭлементыПорядокУсловий(Форма)
	
	Префикс = ИТК_КонструкторЗапросовКлиентСервер.Префикс();
	Элементы = Форма.Элементы;
	Элемент = Элементы["Conditions"];

	ИмяДействия = ИТК_КонструкторЗапросовКлиентСервер.ОсновноеИмяДействия();
	ИмяКоманднойПанели = Префикс + "УсловияКоманднаяПанель";
	ИмяКонтекстногоМеню = Префикс + "УсловияКонтекстноеМеню";
	ИмяГруппы = "ГруппаПорядок";
	
	ГруппаКоманднойПанели = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманднойПанели + ИмяГруппы, "Группа", , Элемент.КоманднаяПанель);
	ГруппаКонтекстногоМеню = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКонтекстногоМеню + ИмяГруппы, "Группа", , Элемент.КонтекстноеМеню);
	
	// Сдвинуть вверх
	ИмяКоманды = Префикс + "СдвинутьУсловиеВверх";
	
	ПараметрыКоманды = Новый Структура;
	ПараметрыКоманды.Вставить("Картинка", БиблиотекаКартинок.ПереместитьВверх);
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, НСтр("ru = 'Переместить вверх';
																	|en = 'Move up'"), ПараметрыКоманды);
											
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманднойПанели + ИмяКоманды, "Кнопка", Поля, ГруппаКоманднойПанели);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКонтекстногоМеню + ИмяКоманды, "Кнопка", Поля, ГруппаКонтекстногоМеню);
	
	// Сдвинуть вниз
	ИмяКоманды = Префикс + "СдвинутьУсловиеВниз";

	ПараметрыКоманды = Новый Структура;
	ПараметрыКоманды.Вставить("Картинка", БиблиотекаКартинок.ПереместитьВниз);
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, НСтр("ru = 'Переместить вниз';
																	|en = 'Move down'"), ПараметрыКоманды);
											
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманднойПанели + ИмяКоманды, "Кнопка", Поля, ГруппаКоманднойПанели);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКонтекстногоМеню + ИмяКоманды, "Кнопка", Поля, ГруппаКонтекстногоМеню);
	
КонецПроцедуры

Процедура ДобавитьКомандуПерейтиКПолюВВыбранных(Форма)
	
	Префикс = ИТК_КонструкторЗапросовКлиентСервер.Префикс();
	Элементы = Форма.Элементы;

	ИмяДействия = ИТК_КонструкторЗапросовКлиентСервер.ОсновноеИмяДействия();
	
	ИмяКоманды = Префикс + "ПерейтиКПолюВВыбранных";
	ЗаголовокКоманды = НСтр("ru = 'Перейти к полю в выбранных';
							|en = 'Go to field in selected'");
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, ЗаголовокКоманды);
											
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды, "Кнопка", Поля, Элементы["Aliases"].КонтекстноеМеню);
	
КонецПроцедуры

Процедура ДобавитьКомандуМетаданные(Форма, ИмяЭлемента)
	
	Префикс = ИТК_КонструкторЗапросовКлиентСервер.Префикс();
	Элементы = Форма.Элементы;
	
	Инструмент = ИТК_Перечисления.ИнструментМетаданные();
	
	ИмяКоманды = Префикс + "Метаданные_" + ИмяЭлемента;
	ЗаголовокКоманды = ИТК_ТуллкитКлиентСервер.ПредставлениеИнструмента(Инструмент);
	
	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить("Картинка", ИТК_ТуллкитКлиентСервер.КартинкаИнструмента(Инструмент));
	
	ИмяДействия = ИТК_КонструкторЗапросовКлиентСервер.ОсновноеИмяДействия();
	ИТК_Форма.ДобавитьКоманду(Форма, ИмяКоманды, ИмяДействия, ЗаголовокКоманды, ДополнительныеПараметры);
											
	Поля = Новый Структура("ИмяКоманды", ИмяКоманды);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяКоманды, "Кнопка", Поля, Элементы[ИмяЭлемента].КонтекстноеМеню);
	
КонецПроцедуры

Функция ДанныеСвязей(Источники)
	
	ЕстьСвязи = Ложь;
	Для Каждого Источник Из Источники Цикл 
		
		Если ЗначениеЗаполнено(Источник.Соединения) Тогда
			
			ЕстьСвязи = Истина;
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Новый Структура("ЕстьСвязи, Источников", ЕстьСвязи, Источники.Количество());
	
КонецФункции

Процедура ДобавитьРеквизитыЭлементыСвязей(Форма)
	
	ИмяРеквизитаСвязи = ИТК_КонструкторЗапросовКлиентСервер.ИмяРеквизитаСвязи();
	Элементы = Форма.Элементы;
	
	Элементы["Group13"].Видимость = Ложь;
	Элементы["Joins"].Видимость = Ложь;
	
	// Создание реквизитов
	ДобавляемыеРеквизиты = Новый Массив;
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИмяРеквизитаСвязи, "ТаблицаЗначений");
	
	Для Сч = 1 По 2 Цикл 
		
		Заголовок = НСтр("ru = 'Таблица';
						|en = 'Table'") + " " + Сч;
		ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, "Таблица" + Сч, ИТК_ТипыКлиентСервер.ОписаниеИмяМетаданных(), Заголовок, ИмяРеквизитаСвязи);
		
		Заголовок = НСтр("ru = 'Все';
						|en = 'All'") + " " + Сч;
		ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, "Все" + Сч, "Булево", Заголовок, ИмяРеквизитаСвязи);
		
		ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, "Картинка" + Сч, "Картинка", , ИмяРеквизитаСвязи);
		
	КонецЦикла;
	
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, "ИндексСтроки", ИТК_ТипыКлиентСервер.ОписаниеЧисло(10), , ИмяРеквизитаСвязи);
	
	Заголовок = НСтр("ru = 'Условие';
					|en = 'Condition'");
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, "Условие", ИТК_ТипыКлиентСервер.ОписаниеСтрока(), Заголовок, ИмяРеквизитаСвязи);
	
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	// Создание элементов
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", ИмяРеквизитаСвязи);
	Поля.Вставить("ИзменятьПорядокСтрок", Ложь);
	Поля.Вставить("АвтоВводНовойСтроки", Ложь);
	Поля.Вставить("РазрешитьНачалоПеретаскивания", Ложь);
	Поля.Вставить("РазрешитьПеретаскивание", Ложь);
	Поля.Вставить("РежимВыделения", РежимВыделенияТаблицы.Одиночный);
	
	ТаблицаСвязи = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи, "Таблица", Поля, Элементы["JoinsPage"]);
	УстановитьДействиеСвязей(ТаблицаСвязи, "ПриНачалеРедактирования");
	УстановитьДействиеСвязей(ТаблицаСвязи, "ПередОкончаниемРедактирования");
	УстановитьДействиеСвязей(ТаблицаСвязи, "ПриОкончанииРедактирования");
	УстановитьДействиеСвязей(ТаблицаСвязи, "ПередУдалением");
	
	ДобавитьГруппуСвязиТаблица(Форма, ИмяРеквизитаСвязи, ТаблицаСвязи, 1);
	ДобавитьГруппуСвязиТаблица(Форма, ИмяРеквизитаСвязи, ТаблицаСвязи, 2);
	
	// Колонка Условие
	ИмяКолонки = "Условие";
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", ИмяРеквизитаСвязи + "." + ИмяКолонки);
	Поля.Вставить("МногострочныйРежим", Истина);
	Поля.Вставить("КнопкаВыбора", Истина);
	КолонкаУсловие = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи + ИмяКолонки, "ПолеВвода", Поля, ТаблицаСвязи);
	УстановитьДействиеСвязей(КолонкаУсловие, "НачалоВыбора");
	
КонецПроцедуры

Процедура ДобавитьГруппуСвязиТаблица(Форма, ИмяРеквизитаСвязи, ТаблицаСвязи, Номер)
	
	// Таблица
	ИмяКолонки = "Таблица" + Номер;
	
	Поля = Новый Структура;
	Поля.Вставить("Группировка", ГруппировкаКолонок.ВЯчейке);
	ГруппаТаблица = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи + "Группа" + ИмяКолонки, "Группа", Поля, ТаблицаСвязи);
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", ИмяРеквизитаСвязи + "." + ИмяКолонки);
	Поля.Вставить("Ширина", 25);
	Поля.Вставить("АвтоОтметкаНезаполненного", Истина);
	Поля.Вставить("КнопкаВыбора", Истина);
	Поля.Вставить("РедактированиеТекста", Ложь);
	КолонкаТаблица = ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи + ИмяКолонки, "ПолеВвода", Поля, ГруппаТаблица);
	УстановитьДействиеСвязей(КолонкаТаблица, "НачалоВыбора");
	
	// Картинка
	ИмяКолонки = "Картинка" + Номер;
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", ИмяРеквизитаСвязи + "." + ИмяКолонки);
	Поля.Вставить("ПоложениеЗаголовка", ПоложениеЗаголовкаЭлементаФормы.Нет);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи + ИмяКолонки, "ПолеВвода", Поля, ГруппаТаблица);
	
	// Все
	ИмяКолонки = "Все" + Номер;
	
	Поля = Новый Структура;
	Поля.Вставить("ПутьКДанным", ИмяРеквизитаСвязи + "." + ИмяКолонки);
	ИТК_Форма.ДобавитьЭлемент(Форма, ИмяРеквизитаСвязи + ИмяКолонки, "ПолеФлажка", Поля, ТаблицаСвязи);
	
КонецПроцедуры

Процедура ЗаполнениеСвязей(Форма)
	
	Связи = Форма[ИТК_КонструкторЗапросовКлиентСервер.ИмяРеквизитаСвязи()];
	Связи.Очистить();
	
	МодульОбъекта = Форма.РеквизитФормыВЗначение("Object");
	СхемаЗапроса = ПолучитьИзВременногоХранилища(Форма["QueryWizardAddress"]);
	Запрос = МодульОбъекта.GetSchemaQuery(СхемаЗапроса, Форма["CurrentQuerySchemaSelectQuery"], Форма["NestedQueryPositionAddress"]);
	
	Операторы = Запрос.Операторы;
	CurrentQuerySchemaOperator = Форма["CurrentQuerySchemaOperator"];
	Если CurrentQuerySchemaOperator >= Операторы.Количество() Тогда
		CurrentQuerySchemaOperator = 0;
	КонецЕсли;

	Оператор = Операторы.Получить(CurrentQuerySchemaOperator);
	
	Для Каждого ОписаниеИсточника Из Оператор.Источники Цикл 
		
		Источник = ОписаниеИсточника.Источник;
		Таблица1 = Источник.Псевдоним;
		
		ПредыдущаяТаблица2 = "";
		Для Каждого ОписаниеСоединения Из ОписаниеИсточника.Соединения Цикл 
			
			ТипСоединения = ОписаниеСоединения.ТипСоединения;
			ИсточникаСоединения = ОписаниеСоединения.Источник.Источник;
			
			Таблица2 = ИсточникаСоединения.Псевдоним;
			Если ПредыдущаяТаблица2 <> Таблица2 Тогда
				
				НоваяСтрока = Связи.Добавить();
				НоваяСтрока.Картинка1 = ИТК_КонструкторЗапросов.КартинкаИсточникаСвязи(Источник);
				НоваяСтрока.Таблица1 = Таблица1;
				НоваяСтрока.Все1 = (ТипСоединения = ТипСоединенияСхемыЗапроса.ЛевоеВнешнее ИЛИ ТипСоединения = ТипСоединенияСхемыЗапроса.ПолноеВнешнее);
				НоваяСтрока.Картинка2 = ИТК_КонструкторЗапросов.КартинкаИсточникаСвязи(ИсточникаСоединения);
				НоваяСтрока.Таблица2 = Таблица2;
				НоваяСтрока.Все2 = (ТипСоединения = ТипСоединенияСхемыЗапроса.ПравоеВнешнее	ИЛИ ТипСоединения = ТипСоединенияСхемыЗапроса.ПолноеВнешнее);
				НоваяСтрока.Условие = ОписаниеСоединения.Условие;
				
			Иначе
				
				НоваяСтрока.Условие = НоваяСтрока.Условие + " И " + ОписаниеСоединения.Условие;
				
			КонецЕсли;
			
			ПредыдущаяТаблица2 = НоваяСтрока.Таблица2;
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьДействиеСвязей(Элемент, ИмяДействия)
	
	ИмяОбработчика = "Подключаемый" + Элемент.Имя + ИмяДействия;
	Элемент.УстановитьДействие(ИмяДействия, ИмяОбработчика);
	
КонецПроцедуры

Процедура СобратьДанныеИсточников(Источник, Результат, Кэш, ИмяПараметраВиртуальнойТаблицы = "")

	// Основной источник
	ОсновнойИсточник = Источник.Источник;
	
	ПараметрВиртуальнойТаблицы = ЗначениеЗаполнено(ИмяПараметраВиртуальнойТаблицы);
	Если ПараметрВиртуальнойТаблицы Тогда
		
		ДоступныеПоля = ДоступныеПоляВиртуальныхТаблицИсточника(ОсновнойИсточник, ИмяПараметраВиртуальнойТаблицы);
		Для Каждого ИмяПоля Из ДоступныеПоля Цикл 
			
			Поле = ОсновнойИсточник.ДоступныеПоля.Найти(ИмяПоля);
			
			ОписаниеПоля = ИТК_РедакторКода.ОписаниеПользовательскогоОбъекта(Кэш, Поле.ТипЗначения);
			Результат.Вставить(ИмяПоля, ОписаниеПоля); 
			
		КонецЦикла;
		
	Иначе
		
		Поля = Новый Структура;
		Для Каждого Поле Из ОсновнойИсточник.ДоступныеПоля Цикл 
			
			Если ТипЗнч(Поле) = Тип("ДоступнаяВложеннаяТаблицаСхемыЗапроса") Тогда
				Продолжить;
			КонецЕсли;
			
			ИмяПоля = Поле.Имя;
			Если СтрНайти(ИмяПоля, ".") Тогда
				Продолжить;
			КонецЕсли;
			
			Поля.Вставить(ИмяПоля, Поле.ТипЗначения);
			
		КонецЦикла;
		
		ОписаниеПолей = ИТК_РедакторКода.ОписаниеПользовательскогоОбъекта(Кэш, , Поля);
		Результат.Вставить(ОсновнойИсточник.Псевдоним, ОписаниеПолей); 
		
	КонецЕсли;
	
	// Соединения
	Для Каждого Соединение Из Источник.Соединения Цикл 
		СобратьДанныеИсточников(Соединение.Источник, Результат, Кэш);
	КонецЦикла;
	
КонецПроцедуры

Процедура ДополнитьДанныеИсточниковВходнымиВнешнимиИсточниками(Источники, Источник, ЗначенияВнешнихИсточников)
	
	// Основной источник
	ОсновнойИсточник = Источник.Источник;
	Если ТипЗнч(ОсновнойИсточник) = Тип("ОписаниеВременнойТаблицыСхемыЗапроса") Тогда
		
		ЗначениеВнешнегоИсточника = Неопределено;
		Для Каждого ОписаниеВнешнегоИсточника Из ЗначенияВнешнихИсточников Цикл 
			
			Если ОписаниеВнешнегоИсточника.Ключ <> ОсновнойИсточник.ИмяТаблицы Тогда
				Продолжить;
			КонецЕсли;
			
			ДоступныеПоля = ОсновнойИсточник.ДоступныеПоля;
			ВнешнийИсточник = ПолучитьИзВременногоХранилища(ОписаниеВнешнегоИсточника.Значение);
			
			// Дополним поля источника
			Для Каждого Колонка Из ВнешнийИсточник.Колонки Цикл 
				
				Если ДоступныеПоля.Найти(Колонка.Имя) = Неопределено Тогда
					ДоступныеПоля.Вставить(ВнешнийИсточник.Колонки.Индекс(Колонка), Колонка.Имя, Колонка.ТипЗначения);
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;

	КонецЕсли;
	
	// Соединения
	Для Каждого Соединение Из Источник.Соединения Цикл 
		ДополнитьДанныеИсточниковВходнымиВнешнимиИсточниками(Соединение, Соединение.Источник, ЗначенияВнешнихИсточников);
	КонецЦикла;
	
КонецПроцедуры

Функция NameВременныеТаблицыВходные()
	
	Возврат НСтр("ru = 'Временные таблицы (входные)';
				|en = 'Temporary tables (input)'");
	
КонецФункции

Функция ДоступныеПоляВиртуальныхТаблицИсточника(Источник, ИмяПараметраВиртуальнойТаблицы)
	
	ПолноеИмяТаблицыИсточника = Источник.ИмяТаблицы;
	ИмяКоллекции = ИТК_Метаданные.ИмяКоллекции(ПолноеИмяТаблицыИсточника);
	ИмяВиртуальнойТаблицы = ИТК_Строки.РазборПрочитатьДоСимвола(ПолноеИмяТаблицыИсточника, ".", , НаправлениеПоиска.СКонца);
	
	Результат = Новый Массив;
	Если ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрНакопления(ИмяКоллекции)
			ИЛИ (ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрБухгалтерии(ИмяКоллекции)
					И ИмяПараметраВиртуальнойТаблицы = "Условие"
					И (ИмяВиртуальнойТаблицы = "ОборотыДтКт"
						ИЛИ ИмяВиртуальнойТаблицы = "Остатки"
						ИЛИ ИмяВиртуальнойТаблицы = "ОстаткиИОбороты")) Тогда
		
		Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
			
			Если НЕ Поле.Роль.Измерение Тогда
				Продолжить;
			КонецЕсли;
			
			Результат.Добавить(Поле.Имя);
			
		КонецЦикла;
		
	ИначеЕсли ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрСведений(ИмяКоллекции)
				ИЛИ (ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрРасчета(ИмяКоллекции)
						И ИмяВиртуальнойТаблицы = "ФактическийПериодДействия") Тогда
		
		Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
			Результат.Добавить(Поле.Имя);
		КонецЦикла;
		
	ИначеЕсли ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрРасчета(ИмяКоллекции) Тогда
		
		Если ИмяВиртуальнойТаблицы = "База" Тогда
			
			Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
				
				ИмяПоля = Поле.Имя;
				
				Если СтрЗаканчиваетсяНа(ИмяПоля, "Разрез")
						ИЛИ СтрЗаканчиваетсяНа(ИмяПоля, "База") Тогда
					Продолжить;
				КонецЕсли;
				
				Результат.Добавить(ИмяПоля);
				
			КонецЦикла;
			
		ИначеЕсли ИмяВиртуальнойТаблицы = "ДанныеГрафика" Тогда
			
			Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
				
				ИмяПоля = Поле.Имя;
				
				Если СтрНачинаетсяС(ИмяПоля, "Значение") Тогда
					Продолжить;
				КонецЕсли;
				
				Результат.Добавить(ИмяПоля);
				
			КонецЦикла;
			
		КонецЕсли;
		
	ИначеЕсли ИТК_МетаданныеКлиентСерверПовтИсп.ЭтоРегистрБухгалтерии(ИмяКоллекции) Тогда
		
		Если ИмяВиртуальнойТаблицы = "ДвиженияССубконто" Тогда
			
			Если ИмяПараметраВиртуальнойТаблицы = "Упорядочивание" Тогда
				
				Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
					
					ИмяПоля = Поле.Имя;
					
					Если СтрНачинаетсяС(ИмяПоля, "Субконто")
							ИЛИ СтрНачинаетсяС(ИмяПоля, "ВидСубконто")
							ИЛИ ИмяПоля = "МоментВремени" Тогда
						Продолжить;
					КонецЕсли;
					
					Результат.Добавить(ИмяПоля);
					
				КонецЦикла;
				
			Иначе
				
				Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
					
					ИмяПоля = Поле.Имя;
					
					Если ИмяПоля = "МоментВремени" Тогда
						Продолжить;
					КонецЕсли;
					
					Результат.Добавить(ИмяПоля);
					
				КонецЦикла;
				
			КонецЕсли;
			
		ИначеЕсли ИмяВиртуальнойТаблицы = "Обороты"
					ИЛИ ИмяВиртуальнойТаблицы = "ОборотыДтКт"
					ИЛИ ИмяВиртуальнойТаблицы = "Остатки" Тогда
			
			Если ИмяПараметраВиртуальнойТаблицы = "УсловиеСчета" Тогда
				ИмяПоляСчет = "Счет";
			ИначеЕсли ИмяПараметраВиртуальнойТаблицы = "КорСчет" Тогда
				ИмяПоляСчет = "КорСчет";
			ИначеЕсли ИмяПараметраВиртуальнойТаблицы = "УсловиеСчетаДт" Тогда
				ИмяПоляСчет = "СчетДт";
			ИначеЕсли ИмяПараметраВиртуальнойТаблицы = "УсловиеСчетаКт" Тогда
				ИмяПоляСчет = "СчетКт";
			Иначе
				ИмяПоляСчет = Неопределено;
			КонецЕсли;
			
			Для Каждого Поле Из Источник.ДоступныеПоля Цикл 
				
				ИмяПоля = Поле.Имя;
				Если ИмяПоля = ИмяПоляСчет Тогда
					Результат.Добавить(ИмяПоля);
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция РежимКомпоновкиДанныхФормы(Форма)
	
	ИмяРеквизита = "DataCompositionMode";
	
	Реквизит = ИТК_Форма.НайтиРеквизит(Форма, ИмяРеквизита);
	Если Реквизит = Неопределено Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Форма[ИмяРеквизита];
	
КонецФункции

Процедура ФормаОсновнаяПереводНаАнглийский(Форма)
	
	Если ИТК_ОбщийКлиентСервер.КодТекущегоЯзыка() <> ИТК_ОбщийКлиентСервер.КодЯзыкаАнглийский() Тогда 
		Возврат;
	КонецЕсли;
	
	// Заголовки страниц
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TablesAndFieldsPage", НСтр("en = 'Tables and fields'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "JoinsPage", НСтр("en = 'Joins'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "GroupingPage", НСтр("en = 'Grouping'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "ConditionsPage", НСтр("en = 'Conditions'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AdditionallyPage", НСтр("en = 'Additionally'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "UnionsAliasesPage", НСтр("en = 'Unions/Aliases'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "IndexPage", НСтр("en = 'Index'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "OrderPage", НСтр("en = 'Order'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsPage", НСтр("en = 'Totals'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "DropTablePage", НСтр("en = 'Drop table'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "DataCompositionPage", НСтр("en = 'DataComposition'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "CharacteristicsPage", НСтр("en = 'Characteristics'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "QueryBatchPage", НСтр("en = 'Query batch'"));

	// Кнопки перенести, удалить
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SourcesAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SourcesAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SourcesDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SourcesDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AvailableFieldsAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AvailableFieldsAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AvailableFieldsDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AvailableFieldsDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "GroupAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "GroupAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "GroupDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "GroupDeleteAll", НСтр("en = '<<'"));
	
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SummAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SummAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SummDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "SummDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "ConditionAddFromFields", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "ConditionAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "ConditionDelete1", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "ConditionDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AdditionallyAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AdditionallyAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AdditionallyDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "AdditionallyDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "IndexAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "IndexAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "IndexDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "IndexDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "OrderAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "OrderAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "OrderDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "OrderDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsFieldsAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsFieldsAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsFieldsDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsFieldsDeleteAll", НСтр("en = '<<'"));

	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsExpressionsAdd", НСтр("en = '>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsExpressionsAddAll", НСтр("en = '>>'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsExpressionsDelete", НСтр("en = '<'"));
	УстановитьЗаголовокЭлементаСПроверкой(Форма, "TotalsExpressionsDeleteAll", НСтр("en = '<<'"));
	
	СписокВыборкаТипЗапроса = Форма.Элементы.QueryType.СписокВыбора;
	СписокВыборкаТипЗапроса.Очистить();
	СписокВыборкаТипЗапроса.Добавить(0, НСтр("en = 'Data sampling'"));
	СписокВыборкаТипЗапроса.Добавить(1, НСтр("en = 'Creating a temporary table'"));
	Если ИТК_ОбщийКлиентСервер.ПоддерживаетсяПлатформой("8.3.25") Тогда
		СписокВыборкаТипЗапроса.Добавить(2, НСтр("en = 'Adding to a temporary table'"));
		СписокВыборкаТипЗапроса.Добавить(3, НСтр("en = 'Destroying a temporary table'"));
	КонецЕсли;

КонецПроцедуры

Процедура УстановитьЗаголовокЭлементаСПроверкой(Форма, ИмяЭлементы, Заголовок)
	
	НайденныйЭлемент = Форма.Элементы.Найти(ИмяЭлементы);
	Если НайденныйЭлемент = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	НайденныйЭлемент.Заголовок = Заголовок;
	
КонецПроцедуры

#КонецОбласти
