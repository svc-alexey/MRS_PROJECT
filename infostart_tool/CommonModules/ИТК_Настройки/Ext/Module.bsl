#Область ПрограммныйИнтерфейс

Процедура ИнициализироватьФорму(Форма, ВидИнструмента = Неопределено) Экспорт
	
	ИмяРеквизитаНастройкиОбщие = ИТК_НастройкиКлиентСервер.ИмяРеквизитаНастройкиОбщие();
	ИмяРеквизитаНастройки = ИТК_НастройкиКлиентСервер.ИмяРеквизитаНастройки();
	
	// Добавляем реквизиты
	ДобавляемыеРеквизиты = Новый Массив;
	
	ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИмяРеквизитаНастройкиОбщие);
	
	Если ЗначениеЗаполнено(ВидИнструмента) Тогда
		
		ИТК_Форма.ДобавитьРеквизит(ДобавляемыеРеквизиты, ИмяРеквизитаНастройки);
		
	КонецЕсли;
	
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	// Загружаем настройки
	Форма[ИмяРеквизитаНастройкиОбщие] = Загрузить();
	
	Если ЗначениеЗаполнено(ВидИнструмента) Тогда
		
		Форма[ИмяРеквизитаНастройки] = Загрузить(ВидИнструмента);
		
	КонецЕсли;
	
КонецПроцедуры

Функция Загрузить(ВидИнструмента = Неопределено) Экспорт
	
	КлючОбъекта = ИТК_НастройкиКлиентСервер.КлючОбъекта();
	КлючНастроек = ИТК_НастройкиКлиентСерверПовтИсп.КлючНастроек(ВидИнструмента);
	Результат = ХранилищеСистемныхНастроек.Загрузить(КлючОбъекта, КлючНастроек);
	
	Если Результат = Неопределено Тогда
		Результат = Новый Структура;
	КонецЕсли;
	
	Для Каждого ОписаниеНастройки Из ИТК_НастройкиКлиентСерверПовтИсп.Список(ВидИнструмента) Цикл 
		
		Имя = ОписаниеНастройки.Ключ;
		
		Значение = Неопределено;
		СуществуетСвойство = Результат.Свойство(Имя, Значение);
		ЗначениеПоУмолчанию = (НЕ СуществуетСвойство ИЛИ Значение = Неопределено);
		Если ЗначениеПоУмолчанию Тогда
			Значение = ОписаниеНастройки.Значение;
		КонецЕсли;
		
		// Подготовка значения (преобразование)
		РасшифроватьЗначение(Имя, Значение);
		ПреобразованиеПеречисления(Имя, Значение, Истина);
		ПреобразованиеПрочие(Имя, Значение);
		
		Результат.Вставить(Имя, Значение);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура Сохранить(Настройки, ВидИнструмента = Неопределено) Экспорт
	
	Для Каждого ОписаниеНастройки Из Настройки Цикл 
		
		Имя = ОписаниеНастройки.Ключ;
		Значение = ОписаниеНастройки.Значение;
		
		// Подготовка значения (преобразование)
		ЗашифроватьЗначение(Имя, Значение);
		ПреобразованиеПеречисления(Имя, Значение);
		
		Настройки.Вставить(Имя, Значение);
		
	КонецЦикла;
	
	ХранилищеСистемныхНастроек.Сохранить(ИТК_НастройкиКлиентСервер.КлючОбъекта(), ИТК_НастройкиКлиентСерверПовтИсп.КлючНастроек(ВидИнструмента), Настройки);
	
КонецПроцедуры

Функция ЗначениеОбщейНастройки(Форма, Имя) Экспорт
	
	Возврат ИТК_НастройкиКлиентСервер.ЗначениеОбщейНастройки(Форма, Имя);
	
КонецФункции

Функция ЗначениеНастройкиВФорме(Форма, Имя) Экспорт
	
	Возврат ИТК_НастройкиКлиентСервер.ЗначениеНастройки(Форма, Имя);
	
КонецФункции

Функция Значение(Группа, Имя) Экспорт
	
	КлючОбъекта = ИТК_НастройкиКлиентСерверПовтИсп.КлючНастроек(Группа);
	Результат = ХранилищеСистемныхНастроек.Загрузить(КлючОбъекта, Имя);
	
	Если Результат = Неопределено Тогда
		
		// Значение по умолчанию
		НастройкиГруппы = ИТК_НастройкиКлиентСерверПовтИсп.Список(Группа);
		Возврат НастройкиГруппы[Имя];

	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаписатьЗначение(Группа, Имя, Значение) Экспорт
	
	КлючОбъекта = ИТК_НастройкиКлиентСерверПовтИсп.КлючНастроек(Группа);
	ХранилищеСистемныхНастроек.Сохранить(КлючОбъекта, Имя, Значение);
	
КонецПроцедуры

Функция ЗагрузитьВсе(ВидИнструмента = Неопределено) Экспорт 
	
	Результат = Новый Структура;
	Результат.Вставить(ИТК_НастройкиКлиентСервер.ИмяРеквизитаНастройкиОбщие(), Загрузить());
	
	Если ЗначениеЗаполнено(ВидИнструмента) Тогда
		
		Результат.Вставить(ИТК_НастройкиКлиентСервер.ИмяРеквизитаНастройки(), Загрузить(ВидИнструмента));
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПреобразованиеПеречисления(Имя, Значение, Обратное = Ложь)
	
	Если Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиРасшифровкаТаблицЗначений() Тогда
		
		Значение = ИТК_НастройкиКлиентСерверПовтИсп.ПреобразованиеПеречисленияРасшифровкаТаблицЗначений(Значение, Обратное);
		
	ИначеЕсли Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиКонструкторЗапросаТонкогоКлиента()
			ИЛИ Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиКонструкторЗапросаТолстогоКлиента() Тогда
			
		Значение = ИТК_НастройкиКлиентСерверПовтИсп.ПреобразованиеПеречисленияКонструкторЗапроса(Значение, Обратное);
		
	ИначеЕсли Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиКонструкторСКДТолстогоКлиента() Тогда
		
		Значение = ИТК_НастройкиКлиентСерверПовтИсп.ПреобразованиеПеречисленияКонструкторСКД(Значение, Обратное);
		
	ИначеЕсли Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиПолучениеПлана() Тогда
		
		Значение = ИТК_НастройкиКлиентСерверПовтИсп.ПреобразованиеПеречисленияПолучениеПлана(Значение, Обратное);
		
	ИначеЕсли Имя = ИТК_НастройкиКлиентСерверПовтИсп.ИмяНастройкиСохранятьДанныеПоУмолчаниюВ() Тогда
		
		Значение = ИТК_НастройкиКлиентСерверПовтИсп.ПреобразованиеПеречисленияТипХраненияДанных(Значение, Обратное);
		
	ИначеЕсли Имя = "ИзбранныеРегламентныеЗадания" Тогда
		
		Если ТипЗнч(Значение) = Тип("Массив") Тогда
			
			НовоеЗначение = ИТК_ИзбранноеКлиентСервер.КоллекцияПоУмолчанию();
			Для Каждого ЭлементЗначения Из Значение Цикл 
				НовоеЗначение.Вставить(ЭлементЗначения, Истина);
			КонецЦикла;
			
			Значение = НовоеЗначение;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗашифроватьЗначение(Имя, Знач Значение)
	
	Если ТипЗнч(Значение) = Тип("СписокЗначений") Тогда
		
		Для Каждого ЭлементСписка Из Значение Цикл 
			
			ЗашифроватьЗначение(Имя, ЭлементСписка.Значение);
			
		КонецЦикла;
		
	ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
		
		Для Каждого ЭлементСтруктуры Из Значение Цикл 
			
			ЗашифроватьЗначение(ЭлементСтруктуры.Ключ, ЭлементСтруктуры.Значение);
			
		КонецЦикла;
		
	Иначе
		
		ЗашифроватьЗначениеПоля(Имя, Значение);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура РасшифроватьЗначение(Имя, Знач Значение)
	
	Если ТипЗнч(Значение) = Тип("СписокЗначений") Тогда
		
		Для Каждого ЭлементСписка Из Значение Цикл 
			РасшифроватьЗначение(Имя, ЭлементСписка.Значение);
		КонецЦикла;
		
	ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
		
		Для Каждого ЭлементСтруктуры Из Значение Цикл 
			РасшифроватьЗначение(ЭлементСтруктуры.Ключ, ЭлементСтруктуры.Значение);
		КонецЦикла;
		
	Иначе
		РасшифроватьЗначениеПоля(Имя, Значение);
	КонецЕсли;
	
КонецПроцедуры

Процедура ЗашифроватьЗначениеПоля(Имя, Значение)
	
	Если ШифруемоеПоле(Имя) Тогда
		Значение = ИТК_Строки.Зашифровать(Значение);
	КонецЕсли;
	
КонецПроцедуры

Процедура РасшифроватьЗначениеПоля(Имя, Значение)
	
	Если ШифруемоеПоле(Имя) Тогда
		Значение = ИТК_Строки.Расшифровать(Значение);
	КонецЕсли;
	
КонецПроцедуры

Функция ШифруемоеПоле(Имя)
	
	Возврат (СтрНайти(Имя, "Пароль"));
	
КонецФункции

Процедура ПреобразованиеПрочие(Имя, Значение)
	
	Если Имя = "ПодключенияККластерам" Тогда
		
		Для Каждого ПодключениеККластеру Из Значение Цикл 
			
			ОписаниеПодключения = ИТК_ПодключениеКСерверамКлиентСервер.ОписаниеПодключения();
			ЗаполнитьЗначенияСвойств(ОписаниеПодключения, ПодключениеККластеру.Значение);
			
			ПодключениеККластеру.Значение = ОписаниеПодключения;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

