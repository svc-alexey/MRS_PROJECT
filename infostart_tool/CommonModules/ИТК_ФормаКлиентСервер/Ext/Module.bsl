#Область ПрограммныйИнтерфейс

// Обновляет заголовок формы
//
// Параметры:
//  Форма - Форма - Форма
//  Заголовок - Строка - Заголовок формы
//  Дополнение - Булево - Дополнять заголовок названием расширения
//
Процедура Заголовок(Форма, Заголовок, Дополнение = Ложь) Экспорт
	
	НовыйЗаголовок = Заголовок;
	
	Если Дополнение Тогда
		НовыйЗаголовок = НовыйЗаголовок + " : " + ИТК_КонсольРазработчикаКлиентСервер.Представление();
	КонецЕсли;
	
	Форма.Заголовок = НовыйЗаголовок;
	
КонецПроцедуры

Функция НеобходимоРазвернутьСтроку(Строка, ПроверяемоеПоле = "Представление") Экспорт
	
	Если Строка = Неопределено Тогда
		
		Результат = Ложь;
		
	Иначе
		
		Строки = Строка.ПолучитьЭлементы();
		Результат = ЗначениеЗаполнено(Строки) И Не ЗначениеЗаполнено(Строки[0][ПроверяемоеПоле]);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УровеньРазворачиваемойСтроки(Строка) Экспорт
	
	Результат = 0;
	Родитель = Строка;
	
	Пока Родитель <> Неопределено Цикл 
		
		Родитель = Родитель.ПолучитьРодителя();
		Результат = Результат + 1;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Устанавливает заголовок формы
//
// Параметры:
//  Форма - Форма - Форма
//  Заголовок - Строка - Заголовок формы
//  Дополнение - Строка - Дополнение заголовка
//
Процедура УстановитьЗаголовок(Форма, Заголовок, Дополнение = "") Экспорт
	
	Если ЗначениеЗаполнено(Дополнение) Тогда
		
		Заголовок = СтрШаблон("%1 : %2", Заголовок, Дополнение);
		
	КонецЕсли;
	
	Форма.Заголовок = Заголовок;
	
КонецПроцедуры

Функция ЗаголовокСКоличеством(Заголовок, Количество) Экспорт
	
	Результат = Заголовок;
	
	Если ЗначениеЗаполнено(Количество) Тогда
		Результат = СтрШаблон("%1 (%2)", Результат, Формат(Количество, "ЧГ="));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаголовокСКоличествомВНачале(Заголовок, Количество) Экспорт
	
	Результат = Заголовок;
	
	Если ЗначениеЗаполнено(Количество) Тогда
		Результат = СтрШаблон("(%2) %1", Результат, Формат(Количество, "ЧГ="));
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗаголовокСРасшифровкой(Заголовок, Расшифровка) Экспорт
	
	Результат = Заголовок;
	
	Если ЗначениеЗаполнено(Расшифровка) Тогда
		
		Результат = СтрШаблон("%1 - %2", Результат, Расшифровка);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ФорматРедактированияДаты(ОписаниеТипов) Экспорт
	
	Если ИТК_ТипыКлиентСервер.Количество(ОписаниеТипов) <> 1 Тогда
		
		Возврат "";
		
	КонецЕсли;
	
	КвалификаторыЧастиДаты = ОписаниеТипов.КвалификаторыДаты.ЧастиДаты;
	
	Если КвалификаторыЧастиДаты = ЧастиДаты.Время Тогда
		
		Результат = "ДЛФ=T";
		
	ИначеЕсли КвалификаторыЧастиДаты = ЧастиДаты.Дата Тогда
		
		Результат = "ДЛФ=D";
		
	Иначе
		
		Результат = "";
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура СкрытьГруппу(Элемент) Экспорт
	
	Если ИТК_ОбщийКлиентСервер.ПоддерживаетсяПлатформой("8.3.12") Тогда
		
		Элемент.Скрыть();
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПоказатьГруппу(Элемент) Экспорт
	
	Если ИТК_ОбщийКлиентСервер.ПоддерживаетсяПлатформой("8.3.12") Тогда
		Элемент.Показать();
	КонецЕсли;
	
КонецПроцедуры

Функция ПредставлениеСписка(Список, Длина = 100, Разделитель = ", ") Экспорт
	
	Если НЕ ЗначениеЗаполнено(Список) Тогда
		Возврат НСтр("ru = 'Все';
					|en = 'All'");
	КонецЕсли;
	
	Результат = "";
	ЭлементЭтоПредставление = (ТипЗнч(Список) = Тип("Строка"))
								ИЛИ ТипЗнч(Список) = Тип("Массив");
	
	Если ТипЗнч(Список) = Тип("Строка") Тогда
		Элементы = СтрРазделить(Список, Разделитель, Ложь);
	Иначе
		Элементы = Список;
	КонецЕсли;
	
	// Собираем элементы
	Для Каждого Элемент Из Элементы Цикл 
		
		// Добавление разделителя
		Если ЗначениеЗаполнено(Результат) Тогда
			Результат = Результат + Разделитель;
		КонецЕсли;
		
		// Добавление элемента
		Если ЭлементЭтоПредставление Тогда
			Представление = Элемент;
		Иначе 
			
			Если ЗначениеЗаполнено(Элемент.Представление) Тогда
				Представление = Элемент.Представление;
			Иначе 
				Представление = Элемент.Значение;
			КонецЕсли;
			
		КонецЕсли;
		
		Результат = Результат + Элемент;
		
		// Анализ максимальной длины
		Если СтрДлина(Результат) > Длина Тогда
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	// Добавлем суффикс
	СуффиксКоличество = "";
	Если Элементы.Количество() > 1 Тогда
		
		Количество = ИТК_Строки.ЧислоВСтроку(Элементы.Количество());
		СуффиксКоличество = СтрШаблон(" (%1)", Количество);
		
	КонецЕсли;
	
	Если СтрДлина(Результат) > Длина Тогда
		
		СуффиксКоличество = ".." + СуффиксКоличество;
		Результат = Лев(Результат, Длина - СтрДлина(СуффиксКоличество));
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СуффиксКоличество) Тогда
		Результат = Результат + СуффиксКоличество;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВыбранОбъект(Объект) Экспорт
	
	Возврат ТипЗнч(Объект) <> Тип("Строка")
				И ЗначениеЗаполнено(Объект);
	
КонецФункции

Функция ВыбранТипОбъекта(Объект) Экспорт
	
	Возврат НЕ (ТипЗнч(Объект) = Тип("Строка") 
					ИЛИ Объект = Неопределено);
	
КонецФункции

Процедура ОтображатьСтрокуПоискаТаблицыФормы(Элемент, ПоложениеПоиска = Неопределено) Экспорт
	
	Если ПоложениеПоиска = Неопределено Тогда
		ПоложениеПоиска = ПоложениеСтрокиПоиска.КоманднаяПанель;
	КонецЕсли;
	
	Если ИТК_ОбщийКлиентСервер.ПоддерживаетсяПлатформой("8.3.16") Тогда
		Элемент.ПоложениеСтрокиПоиска = ПоложениеПоиска;
	КонецЕсли;
	
КонецПроцедуры

Функция ОшибкиНастроекОтчетаИмяОсновногоРеквизита() Экспорт
	
	Возврат "ОшибкиНастроекОтчета";
	
КонецФункции

Процедура ОтображениеСостоянияТабличногоДокумента(Элемент, Текст = Неопределено, Картинка = Неопределено) Экспорт
	
	ОтображениеСостояния = Элемент.ОтображениеСостояния;
	ОтображениеСостояния.Видимость = Истина;
	ОтображениеСостояния.Текст = Текст;
	
	Если Картинка = Неопределено Тогда
		Картинка = БиблиотекаКартинок.ИТК_ДлительнаяОперацияАнимация48;
	КонецЕсли;
	
	ОтображениеСостояния.Картинка = Картинка;
	
КонецПроцедуры

Процедура ОформитьСтрокуПоискаПоПодобию(Элемент, ДополнительныйТекст = Неопределено) Экспорт
	
	Элемент.ОтображениеПодсказки = ОтображениеПодсказки.Кнопка;
	
	Подсказка = НСтр("ru = 'Часть имени или синонима, допустимы несколько частей через пробел
                      |""Ном контр"" и уточнение коллекции через /<ЧастьИмениКоллекции> ""Ном /Спр"".';
					  |en = 'Part of a name or synonym, multiple parts separated by a space are allowed
                      |""Nom contr"" and specification of the collection via /<Part of CollectionName> ""Nom /Ref"".'");
	
	Если ЗначениеЗаполнено(ДополнительныйТекст) Тогда
		Подсказка = Подсказка + Символы.ПС + ДополнительныйТекст;
	КонецЕсли;
	
	Элемент.Подсказка = Подсказка;
	
КонецПроцедуры

Функция ГруппировкаПодчиненныхЭлементовГоризонтальная() Экспорт
	
	Если ИТК_ОбщийКлиентСервер.ПоддерживаетсяПлатформой("8.3.12") Тогда
		ИмяПеречисления = "ГоризонтальнаяВсегда";
	Иначе
		ИмяПеречисления = "Горизонтальная";
	КонецЕсли;
	
	Возврат Вычислить("ГруппировкаПодчиненныхЭлементовФормы." + ИмяПеречисления);
	
КонецФункции

Функция ПанельГиперссылок_УстановитьТекущуюСтраницу(ГруппаЗаголовкиСтраниц, ГруппаСтраницы, ЭлементЗаголовокСтраницы) Экспорт
	
	ИмяСтраницы = ПанельГиперссылок_Имя(ЭлементЗаголовокСтраницы);
	
	// Оформление заголовка и страниц
	Для Каждого ЭлементЗаголовок Из ГруппаЗаголовкиСтраниц.ПодчиненныеЭлементы Цикл 
		
		ШрифтЗаголовка = Новый Шрифт;
		Если ПанельГиперссылок_Имя(ЭлементЗаголовок) = ИмяСтраницы Тогда
			ШрифтЗаголовка = ИТК_ОбщийКлиентСервер.ШрифтЖирный();
		КонецЕсли;
		ЭлементЗаголовок.Шрифт = ШрифтЗаголовка;
		
	КонецЦикла;
	
	// Установка текущей страницы
	Для Каждого ЭлементСтраница Из ГруппаСтраницы.ПодчиненныеЭлементы Цикл 
		
		Если ПанельГиперссылок_Имя(ЭлементСтраница) = ИмяСтраницы Тогда
			ГруппаСтраницы.ТекущаяСтраница = ЭлементСтраница;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
КонецФункции

Функция ОсновнойРодительСтрокиДерева(Строка) Экспорт
	
	Результат = Строка;
	Родитель = Строка.ПолучитьРодителя();
	Пока Родитель <> Неопределено Цикл 
		
		Результат = Родитель;
		Родитель = Родитель.ПолучитьРодителя();
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПанельГиперссылок_Имя(Элемент)
	
	ЧастиИмениСтраницы = СтрРазделить(Элемент.Имя, "_");
	Возврат ЧастиИмениСтраницы[1];
	
КонецФункции

#КонецОбласти
